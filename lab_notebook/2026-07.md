# July 2026

## 2026-07-29/30 · klapmuts census SOLVED (866); CORAL dissected to the RANSAC; fruit S1 fails gates, S2 (protected class) launched
- **Goal:** klapmuts instances -> hierarchy; revive the citrus fruit level.

### klapmuts census — VALIDATED at 866
`sack`@0.1 image-mode (28,587 masks, per-mask SCORES now stored) -> cluster with
5 m gate + vox 0.05/dil 0 -> 501 (failed size audit: 174 row-segments held 90% of
points) -> **1.0 m pitch split -> 866 instances, median longest axis 0.99 m ≈ the
pitch, max 2.75 m, 72% of points plant-scale**. Top-down: beads along every driven
row (figs/klapmuts_instances_topdown.png). Caveats: far-left rows outside the 5 m
gate = unmapped; ~128 two-plant merges; two-sided duplicates NOT deduped.
- Confidence finding: SAM3 score = per-instance x WHOLE-IMAGE presence
  (sam3_image_processor.py:192-198). Scale shifts per frame (max 0.61 vs 0.085,
  same aisle); global thresholds can't work — store scores, threshold post-hoc
  (build_tree_instances now does). Score vs range: r=-0.05 (uncorrelated).
- LiDAR-only segmentation works per-frame: RANSAC ground (sensor 0.69 m above
  floor, tilt <2.4 deg), bag band 0.05-0.65 m -> single bags 0.65-0.74 m wide;
  rows on 1.0 m pitch, 1.6 m row spacing -> site.json.

### site.json convention (sibling of rig.json) + hierarchy debugging saga
Nine baked-in site/hardware constants found this week; now per-dataset:
`<dataset>/site.json` (row_spacing, pitch, plant size, pair range, dir threshold,
CORAL outlier, ground band...). Walk-up loader scripts/site_params.py; absent key
= legacy behaviour (citrus values reproduce hardcodes exactly).
Hierarchy runs 1-4 on the 866: 56 "super-rows" (C++ pairing 1<d<8 m hardcode,
nerf_interface.h:1774) -> site pair range [1.2,2.0] -> 4 pairs (doubled rows
block pairing) -> union-find merge CHAINED (my bug) -> **rows-only mode revealed
the "363-member mega-row" was the OUTLIER BUCKET (label==n_models) miscounted as
a row** + outlier 0.5 rejects 45% (one twin of every two-sided duplicate).
New C++ setter SetCoralOutlierThreshold (rebuild verified; l2c intact).

### CORAL dissected — HighInterface (walk-before-run, Paul's direction)
NEW `interfaces/include/.../high_interface.h`: CORAL exposed on raw (x,z), no
monolithics. **Stage 1 ransac_init: 0.07 s** (vs ~25 min through NerfInterface).
Findings at production params (dir 0.7, thr 1.5): diagonal thieves — dir gate
tolerates 45-deg lines that harvest inlier swaths across rows; no min-inlier gate
(22 junk models to the num_models=40 cap); models refit from the winning 2-POINT
sample, not inlier least-squares. **Tuned (dir 0.985 ≈ ±10 deg, thr = row_spacing/2
= 0.8): 14 clean models 40-66 members, one per physical row, twins absorbed —
row generation essentially solved by the INIT alone**
(figs/klapmuts_ransac_init_tuned.png). Stage 2 fit_rows (init + real
EnergyMinimisation, lambda/beta exposed; production hardcodes BOTH to 0 and runs
5000 fixed iterations) running at lambda=beta=0.5 to clean the 5 stragglers.

### citrus fruit — S1 fails gates; S2 = protected semantic class
- Fruit prep: 2,850 masks / 124 b001 frames (conf 0.05, scores); fruit_v1*.json
  hold NO masks (counts only). paint_fruit_binary.py + v2_C (840 in-inventory
  paints; 831 orphan-tree masks correctly excluded — nodes are INVENTORY,
  masks are hypotheses; Paul's principle).
- **S1** (from scratch + v2_C + 10x fruit px weight): radius 4.174->4.982 (target
  7.1), cross-level pointing 0.0->1.1%. FAIL both gates, mechanism responds.
  Post-mortem: pixel amplification can't beat visible-but-unfired frames' tree
  paint (label conflict), and occlusion only attenuates gradients (never gates).
  S1 = "louder fruit pixels", NOT protected gaussians.
- **S2 launched**: per-gaussian supervision tally read from the rasterizer's own
  blending weights (two autograd.grad probes on the rendered feature map — exact
  w_i, occlusion-aware for free), sticky protection (mass>=10 over >=2 frames),
  anchor to the gaussian's OWN supervising node word (weighted mean of fruit
  targets that touched it — per-node identity, zero bookkeeping). v1 limit:
  tally resets on densification -> protection arms after stop_split_at.
  high_model.py: fruit_protect config block, defaults off.
- FruitNeRF (arXiv 2408.06190) mapping recorded: multi-stage pipeline but
  SINGLE-run training; semantic head detached from density; occlusion handled by
  rendering weights. Paul: no two-stage designs; no speculative fruit nodes.
- 3 failed S-probe launches logged (downscale flag; max-iter < loaded step ->
  empty range trains 0 steps and exits 0; gsplat JIT OOM vs concurrent
  NerfInterface — serialize + MAX_JOBS=4).

### infra
- 381 GB CLIP-cache mystery solved: relative `outputs/{name}_{key}` in
  high_datamanager resolved against CWD + ignored the config's cache_dir field.
  Now: cache lives IN the block dir (clip_cache_<key>/). Old caches orphaned.
- rig.json moved per-dataset (citrus_all/, klapmuts/); rig_calib walks up from
  data dir; --rig flags removed. kf_poses.py: K-indexed pose loading, loud fail.
- **Verdict:** klapmuts census + row generation are in hand; fruit level still
  open pending S2 gates. Commits this session (see git log 2026-07-30).

## 2026-07-28 · klapmuts ingest: SIX silent-default defects; site mapped, census still FAILED
- **Goal:** stand up the klapmuts nursery (polytunnel, potted blueberries, ZED 2i,
  2019 keyframes) through the farm pipeline — masks → 3D instances → hierarchy.
- **Outcome:** trajectory and LiDAR map are now correct and validated. **No validated
  plant census.** Every clustering configuration tried fails. Details below.

### What now works (verified)
- `lio_image_poses_kf20cm.json` REDUMPED: 2018 poses, `image_idx = K` (contiguous
  0..2017), klapmuts ZED extrinsic via `--L2C`, origin-reset frame dropped.
  Command: `src/interfaces/build/temp.linux-x86_64-3.10/bin/dump_lio_image_poses
  --IMAGE_MONO=image_left_kf20cm.monolithic --TRANSFORM_MONO=transform_lio.monolithic
  --NAME_OFFSET=0 --L2C=<klapmuts>`.
  Validation: 100% of poses inside the LiDAR map footprint (was 31%), trajectory runs
  down the centre of every aisle. Path 484 m, median step 0.236 m, max step 0.44 m
  (was 32.76 m). Fig: `figs/klapmuts_traj_vs_lidar_REDUMP.png`.
- `lidar_topdown.npz`: 8,758,795 pts, every 2nd kf, 10 m range clip, 0.1 m cells.
  Height bands: floor −1.0…0 m, PLANT BAND 0.6–2.6 m, tunnel hoops 2.5–3.5 m.
  Plants are individually resolvable in LiDAR alone. Fig: `figs/klapmuts_lidar_topdown.png`.
- SAM3 image-mode, prompt `potted plant`, **conf 0.05**, min_area 1500, 50.1 min:
  138,653 masks / 2019 frames = 68.7 per frame; **empty frames 22 (1.1%)**.

### SAM3 threshold — the citrus operating point does NOT transfer
| conf | masks/frame | empty frames |
|------|-------------|--------------|
| 0.3 (citrus "operating point") | 3.59 | 818 = **40.5%** |
| 0.05 | 68.7 | 22 = **1.1%** |
Per-frame threshold sweep on frames that returned ZERO at 0.3 (kf 1031/496/872/1430):
all recovered 16–69 masks at 0.05, so the instance scores were present and the cut was
too high — not a presence gate. Scores are NOT calibrated across frames: kf 1000 gives
15 masks at the same threshold that gives kf 1031 exactly 0, same aisle.
`blueberry bush` returns 0 masks at every threshold on every frame tested.
**Also falsified:** the skill's claim "0.05 masks ground and sky" — citrus-derived; on
klapmuts masks stay on plants at 0.05 (they over-segment, they do not bleed).

### Clustering — FAILED, both configurations
Collection is now healthy (pose fix): 51,064,592 world points from 135,695 detections,
2034 dropped by the 10 m gate (1.5%). Was 141,241 pts / 333 dets / 95% dropped.
| ground filter | points surviving | clusters | noise |
|---|---|---|---|
| margin 0.6 (default) | 143,538 / 51 M = **0.28%** | 38 | 42.7% |
| `--no-ground-filter` | 51 M (100%) | **13** | 0.0% |
Neither is credible for a site of ~11 rows of potted plants.
**Root cause of the 0.6 failure — a rig assumption, not a tuning value.**
`cluster_tree_instances.py:450` does `keep = pts_y < traj_y − margin`, i.e. keep points
`margin` ABOVE the *camera*, using trajectory height as a ground proxy. Measured over 30
frames / 862,505 in-mask points: camera y = +0.48 median, in-mask lidar y = +0.16 median,
p75 BELOW the camera. The ZED here sits at plant height, so "0.6 m above camera" discards
the plants. On the Jackal the camera is low enough that this approximates canopy — which
is why it never surfaced on citrus. Probe: `/tmp/klap_ground_probe.py`.
**Follow-up (NOT done, needs a method decision):** reference measured ground (the
top-down height map already localises the floor at −1.0…0 m) instead of trajectory height.

### SIX silent-default defects found (the real result of the day)
Each returns a plausible wrong answer with exit code 0:
1. `dump_lio_image_poses` at `./build/bin/` (Jul-6) predates `--L2C` → applied the JACKAL
   extrinsic to a ZED rig → klapmuts poses MIRRORED about x. This is what broke everything.
2. Same extrinsic dead-coded as `build_laser_to_camera()` in `markers_v2_from_sam3.py`.
3. `Sam3Processor(model)` in `build_tree_instances.py` — confidence_threshold never
   exposed, silently 0.5.
4. Pose file chosen by FILENAME: `cluster_tree_instances` / `markers_v2_from_sam3` read
   stream-indexed `lio_image_poses.json` and index it with keyframe K. On klapmuts both
   files were 2019 long so every lookup succeeded and returned the wrong frame.
5. `--skip-existing` was `store_true, default=True` — impossible to disable, and keyed on
   the chunk file existing rather than on the settings that produced it.
6. Ground filter (above).

### Pipeline provenance is BROKEN
`run_unified_pipeline.sh` step 1.1 builds the SAM3 keep-set from `image_name`
(`image_<N+100>.png`, from `dump_lio_image_poses.cpp` NAME_OFFSET=100, preserved through
`keyframe_filter_lio`). `build_tree_instances.list_frames` reads `kf_images/kf_<K>.png`.
Measured intersection on citrus 04: **exactly 0** — the pipeline as written would filter
3543 frames to zero. So 04's existing `sam3_v2` was NOT produced by it; provenance unknown.
`extract_kf_pngs.py`, which creates `kf_images/`, is called by no pipeline and no skill.
**AT RISK:** citrus 04's 85-tree registry was clustered with stream-indexed poses. The fix
changes which pose file is read (3517 vs 3543). UNVERIFIED; the 4D association work depends
on it.

### Code changed (repo root is `aru_sil_core/src`; all of the below UNCOMMITTED)
- NEW `src/scripts/kf_poses.py` — K-indexed pose loader; raises FileNotFoundError with
  remediation instead of silently falling back. `--allow-stream-poses` is the escape hatch.
- NEW `src/config/rigs/{citrus_jackal,klapmuts}.json` + `rig_calib.py` — `citrus_jackal`
  reproduces the historical baked-in extrinsic to **4.4e-16**, so the default is a no-op.
- `build_tree_instances.py`: `--conf` (default 0.3), `--out-name`, `--no-skip-existing`,
  settings recorded in `clips.json`.
- `cluster_tree_instances.py`, `markers_v2_from_sam3.py`: `--rig`, `--out-name`,
  `--allow-stream-poses`; dead `build_laser_to_camera()` deleted; `[calib]` line printed
  before any projection. Also `--cache-points` (collection is ~45 min, everything after is
  seconds) and `--no-ground-filter`; negative margins are now representable.
- Fixed a pre-existing `%`-in-help crash that made `markers_v2_from_sam3.py --help` fail.

### Verdict
Site is mapped and the geometry is trustworthy; the census is not solved and the ground
filter needs a method change, not a sweep. **klapmuts should not keep consuming days — the
December deliverable is the fruit level, which had ZERO progress today (see 2026-07-27
LEVEL COLLAPSE; the 10× fruit up-weight experiment is still unrun).**
All code changes above are UNCOMMITTED (repo root is `aru_sil_core/src`, HEAD 7346534e):
4 modified scripts plus untracked `scripts/kf_poses.py`, `scripts/rig_calib.py` and
`config/` (which holds the rig JSONs). Commit-after-green is overdue — see the June 2026
regression post-mortem.

## 2026-07-23 · Registry audit: clustering undercounts by ~24% (census 112 vs 85)
- **Chain of evidence (Paul-driven):** fruit stats implausible → raw stats: 83/3695 views
  fired, but ~5-px fruit + backlit frames; tiling tested and WORSE (11→5 kept) → imagery:
  18-vs-0 same tree/frame = illumination-dominated → semantic masks: unsegmented sunlit
  wall + obj171/172 both→tree30 → decomposition: "tree 25" = 30 m/5-tree SEGMENT,
  "tree 30" annexes tree 33's lobe → X/O placement map (LaserProjector median-in-bbox):
  7,643 raw dets, 3,695 mapped; unmapped = far-field by design (10 m gate) BUT also whole
  neighbouring blocks (LiDAR reaches 104 m) → **LiDAR cloud census (10 m clip, local
  ground, peak+NMS @3 m):** **112 tree candidates vs 85 registry: 28 missed, 8 confirmed
  multi-peak merges (incl. trees 0, 42), 2 phantom centroids.**
- **Root cause:** voxel-CC clustering (0.1 m, dilate 2) chains canopy-closure bridges
  along rows; registry "trees" are components, not trees. ALL per-tree quantities
  (fruit counts, n_lidar_points, NDVI attribution, ledger identities, associations)
  inherit this. 05's registry (82) built identically → same caveat.
- **Assets built:** cloud_04_topdown(.10m).npz + figs, dets_04_placed.npz, blob census
  script (/tmp — promote per SKILL_CANDIDATES #3/#6).
- **Ops:** 05 fruit queue wrapper had silently died (harness restart) — I wrongly
  reported it running; relaunched for real ~18:30, 82 trees/3,277 views. 04 all-views
  fruit stats: 17/85 trees fired, top counts multi-view consistent (~105 px).
- **Skill hygiene:** citrus-4d-association got a SUPERSEDED banner (offset method
  forbidden); lab_notebook/SKILL_CANDIDATES.md created (7 candidates with caveats).
- **Verdict:** the census map is now the reference truth for 04; clustering rebuild
  (peak/watershed-seeded, pitch prior) is the highest-leverage upstream fix — Paul
  to decide the path.

## 2026-07-23 · OFFSET ASSOCIATION WAS WRONG BY ONE TREE — all associations redone absolute
- **Paul's call:** "the offset approach is fundamentally wrong and has seeped into this,
  remove all offsets and rerun the associations." Correct.
- **Evidence:** (1) trajectories: 01/02/03 row-line positions agree to **0.32 m median
  (max 0.78)** in pure absolute WGS84 — no fitting. (2) Ungameable diagnostic: residual
  translation after independent per-survey anchoring = **1.43 m** vs a **5.87 m tree
  pitch** ⇒ no systematic shift in the absolute frame. (3) Head-to-head: in **all 212**
  cases where offset- and absolute-schemes disagree, their chosen partners are **6.38 m
  apart = one tree pitch**. The offset (fitted to make trees coincide) was circular — its
  tighter 0.79 m pair distance was self-confirming while matching each tree to its NEIGHBOUR.
- **Redone:** `tree_association_abs.py` (absolute, no offsets, ungameable diagnostic
  built in; aru c265b75b). ledger v1 + comparison v1 (ujamaa a1516cf).
- **Impact:** ledger v0's 275 "multi-epoch trees" were WRONG IDENTITIES. v1 has 220
  correct ones (457 canonical). Comparison: survey effect −1.9%→**−0.9%**, anomalies
  40→**23** (14.5%→10.5%) — correct identities measurably reduce spurious change, an
  independent confirmation of the fix.
- **Also today:** 02 trajectory characterised — a SPARSE pass (9 legs @ 31.9 m spacing,
  520 m) vs 01/03 full coverage (18–19 legs @ 6.4 m, 1529/2029 m); expect ~1/5 tree
  coverage in a strip pattern when its SAM3 lands (relevant to G3's ≥90% criterion).
- **Deprecated:** offset_01_to_03.npz, tree_assoc_01_03.npz, tree_association.py,
  ledger_v0/comparison_v0 — superseded, kept on disk for provenance only.
- **Verdict:** keep. The identity layer is now anchored to independently-verified absolute
  GPS. Open: the 1.43 m residual (likely GPS-LIO latency along-track) is worth chasing;
  01/03 NDVI in v1 is unaffected (per-survey sampling never used the offset).

## 2026-07-22 · D2 v0: Comparison faculty over the ledger (275 trees, 36 row cohorts)
- **Goal:** spec §B basics on ledger v0 — per-tree change vs row cohort + anomaly flags.
- **Command/config:** ujamaa/sankofa/compare.py → comparison_v0.json + topdown png
  (figs/). Measure: n_lidar_points log-ratio (canopy PROXY), survey effect removed
  (−1.9% — passes remarkably comparable), robust z vs row-cohort median, flag |z|>2.
- **Result:** 275 trees compared, 40 flagged (14.5% — heavy-tailed vs ~5% Gaussian
  expectation). Top flags (+365%, +206%, +82% vs cohort) are almost certainly cluster
  split/merge artifacts → **v0's anomaly detector is first an association-QA instrument**,
  biology second. Flags cluster at field edges/top row where clustering is least stable.
- **Verdict:** keep — the Comparison loop runs end-to-end on real data. Biological
  version unlocks with: 03/04 NDVI sampling (spectral delta), a true structure metric
  from splats (replaces the proxy), satellite epochs (phenology). The 40 flagged trees
  are a concrete worklist for association re-gating (open question #2).

## 2026-07-22 · D1 SHIPPED: Sankofa ledger v0 (Record faculty live)
- **Goal:** first canonical tree_id × epoch table per SANKOFA_SPEC §A, CPU-only.
- **Command/config:** ujamaa/sankofa/build_ledger.py (adb8dcf); inputs: regenerated
  tree_assoc_01_03.npz (275 mutual pairs @0.79 m — tree_association.py patched to
  persist, aru commit pending push-week rules), sam3_v2 registries (01/03/04),
  tonight's NDVI substrate npzs.
- **Result:** ledger_v0.json — **677 rows, 402 canonical trees, 275 multi-epoch** (both
  13B epochs), strict-JSON valid. Example 13B:154: Jul epoch (S2 NDVI 0.095, 11.5k lidar
  pts) + Aug epoch (7.7k pts, NDVI pending). Pending items in meta; 6 design questions
  in sankofa/LEDGER_OPEN_QUESTIONS.md for Paul (id authority, unmatched trees, non-survey
  epochs, structure column, events source, format).
- **Also tonight:** 02 source bags deleted (Paul) → 274G free; 02 GPS verified TRUE WGS84
  (RTK, 33.9637/-117.3455); 02 kf-monolithic built (2041 kf). GPU E-03: 2 blocks done,
  0 failures.
- **Verdict:** keep — Phase 2's core object exists. Next CPU: 03/04 NDVI runs, NAIP decade
  unroll, D2 comparison basics over the ledger, /row endpoint.

## 2026-07-22 · Evening: interpolation thesis validated; E-03 landing; Sankofa substrate live
- **Interpolation query semantics VALIDATED (Paul's challenge):** on 03_13B_v2G, nesting
  emerged unforced (tangent norms: leaves 5.16 → rows 2.48 → section 1.03) and
  interpolation-path retrieval = 99.26% (= flat retrieval). Row query = walk the pixel's
  geodesic rootward, match row anchors in hyperbolic distance along the path. Symbolic
  route dropped per Paul. Old monitor failed on wrong-radius + decoder-roundtrip, not on
  the interpolation idea. TODO: monitor path-metric upgrade; /row endpoint.
- **E-03 relaunch #2:** first relaunch failed 10/10 on a data-path bug (halves generator
  assumed 01's kf_images layout; 03 keeps json-named frames in images/ — kf_images is
  kf_%06d and 160 short). Generator now probes for the dir containing the json's actual
  names; 80 blocks regenerated (mask_path verified attaching). block_000 COMPLETE with
  v2G pairing (~65 min/block ⇒ ~3 days for all 80; breadth-vs-depth call pending).
- **02 fully ingested + completed:** monolithics, LIO (micro_imu), 4444 poses, 2102 kf;
  kf20cm image monolithic built this evening (2041 kf, 3.1G). GPS check pending (join
  script now unblocked). 05 GPS verified TRUE WGS84 (33.9626,-117.3454), 3386 kf.
- **B4-partial NDVI substrate LIVE** (sankofa_substrate/): offset_01_to_03 regenerated,
  NAIP grid-lock 29→241/286 coverage @0.52m residual, per-tree NDVI (NAIP median 0.433,
  Sentinel-2 series npz), + NAIP archive 2012-2022 = free decade-scale canopy time axis.
- Disk 198G. Uncommitted: automation scripts (not a repo); aru changes committed f9341e2e.

## 2026-07-22 · RESOLVED: graph template validated; instrument was broken, not the model
- **Arc (Paul's three corrections drove it):** monitor's walk-decode Row metric read
  ~random on ckpts whose contrastive hierarchy is near-perfect. Direct child→nearest-row
  RETRIEVAL: c20cos20 = **99.31%** (287/289) vs walk 3.46% — same ckpt. Radial-norm
  forcing (all norms-on runs incl. per-level alignment grid) DESTROYS retrieval
  (99%→4-6%): the constraint fights the contrastive geometry. Batch composition and
  node_types flow verified correct; per-level alignment CLI added (engaged the loss
  ~80×, still no walk-rows — moot now).
- **Template locked:** graph-only trainer + c20cos20 recipe (contrastive 2.0, cos-recon
  2.0, recon 1.0, temp 0.2, --no-level-norms --learn-curv, 1500ep/b16). ~3 min/survey.
- **03_13B_v2G trained + VALIDATED:** retrieval 99.26% (269/272), object decode 99.26%.
- **Fixes landed (aru f9341e2e):** per-level CLI; monitor now prints 'Row (retrieval)'
  as PRIMARY with walk metric annotated; relevancy_service reads HIGH_EMBEDDER_CKPT
  (single pairing knob) + config-aware load (last default-config load fixed).
- **E-03 batch UNPAUSED 15:01 with v2G pairing.** Pairing map: pre-Jul splats ↔ *_v1;
  E-track splats ↔ *_v2G. E-04/05/02 embedder steps use the template recipe (minutes).
- **Provenance note:** test_embeddings.py (Paul's Mac, old monolithic era) likely source
  of historical 95% row figures — per-frame eval; port declined, retrieval metric
  supersedes. Open residue: why 03_13B_v1 (Jun 30) decodes 80.5% objects on the current
  hierarchy (predates text graph per Paul) — unresolved, deferred; v2G makes it moot.

## 2026-07-22 · Graph-only baseline characterized (12-ckpt sweep monitor + eliminations)
- **Goal:** graph-only trainer is the template (Paul); troubleshoot its row performance.
- **Result (monitor on 01 hierarchy, all Jul-7 sweep ckpts + v1):** plain grid: obj
  56–**99.65%** (best `c20cos20`: contrastive 2.0, cos-recon 0.20, recon 1.0, temp 2.0,
  align 0, no radial), rows all 2–5.5%. `_r_` radial variants: rows up to 12.8% but
  objects COLLAPSE to 3–4.5%. v1 ref: 56/2.
- **Eliminated:** (1) monitor row-path unchanged since Jul 7 (git); (2) load mode
  irrelevant — c20cos20 identical (99.65/3.46) under default-config vs config-loaded
  norms; (3) trainer has no separate training-time metric — training tables = this
  monitor. ⇒ no ckpt on disk ever read 95% row-ANCHOR decode.
- **Open fork (for Paul):** if the remembered 95% row figure is DERIVED-row (object →
  its row via hierarchy), c20cos20 already delivers ~99% and its recipe is the template
  as-is. If independent row-anchor decode at 95% is required, that capability doesn't
  exist in any artifact yet and is the actual dev task — radial forcing as swept trades
  objects for rows catastrophically.
- **A1 post-mortem addendum:** my A1 configs (defaults, then v1-matched weights) were
  both outside the working regime the Jul-7 sweep had already mapped. Graph trainer is
  NOT broken — it was mis-driven.

## 2026-07-22 · CORRECTION: text-probe under-reads hierarchy; embedders healthy
- **Context:** monitor_hierarchy on v1 ckpts via HierarchyDataset (graph-only, CLIP TEXT
  node features) read Row 2% (01) / 13% (03), which I mis-attributed to weak radial
  hierarchy. Paul: training-time monitor read 95%+ rows.
- **Resolution round 2 (Paul corrected my "visual vs text" theory; verified in code):**
  MaskFeatureDataset is TEXT features end to end — get_text_embeddings for objects (L294),
  rows (L306), SRs (L324), sections (L331) AND per-mask features (L411-415). No visual
  embeddings exist anywhere in embedder training. So modality does NOT explain 95% vs
  2-13%. The discrepancy is isolated to differences between the proven MaskFeatureDataset
  construction (train_block0 path, monitor read 95%) and the NEW HierarchyDataset
  construction (Jul-13 graph-only code, monitor reads 2-13%) — candidate diffs: view
  synthesis, node_types, drop_super_row, row-vocab construction, or ckpt-load state.
  MECHANISM UNRESOLVED — do not conclude from it.
- **What still stands:** 03_13B_v1 decodes the current 272-obj hierarchy at 80.5%
  object-level (the day's actual question — E-03 pairing is correct). A1 root cause
  reverts to simply: the graph-only trainer/dataset is unvalidated and does not reproduce
  the proven path.
- **RULE (amended):** the HierarchyDataset/graph-only code path — as trainer OR probe —
  produces no conclusions until validated against the proven MaskFeatureDataset path on
  the same survey. Embedder work goes through train_block0.py only.

## 2026-07-22 · WEEK REPORT (late) — autonomous week post-mortem + C1/G2 verdict
- **C1 sky ablation (trained Jul 14–15, evaluated today), 01 b000, eval @19.5k:**
  | variant | full psnr (sky proxy) | psnr_fg |
  | baseline R (black, sky 0.05) | 12.86 | 20.22 |
  | random-bg, 0.05 | 14.81 (+1.95) | 20.06 (−0.16) |
  | black, 0.20 | 12.68 (−0.18) | 19.83 (−0.39) |
  | random-bg, 0.20 | 14.46 (+1.60) | 20.24 (+0.02) |
- **G2 PROVISIONAL PASS — winner: random background.** ~+1.6–2.0 dB full-frame (the
  sky region) at unchanged FG-PSNR; stronger sky-loss alone does nothing. Random-bg
  should become the default for demo-facing blocks. Pending: visual strip render to
  confirm floater removal (metric is a proxy).
- **Week scorecard vs plan:** A-track DONE (G1 FAIL = rollout cancelled, pairing story
  established — decision-grade). Regression fixture recalibrated. R blocks 000–002 kept
  as reference. C1 DONE (above). **B (02/05 ingest + registry), D (Sankofa), E (epoch
  splats): NOT STARTED.** GPU idle ≈6.5 of 7 days.
- **Why (corrected per Paul, 2026-07-22):** the session stayed ALIVE all week. The loop
  froze on PERMISSION PROMPTS — every new tool/command family needed explicit approval,
  and no settings-file change (allowlists, dontAsk, sandbox/MCP keys) defeated the prompt
  layer in this harness. An unattended supervisor cannot self-authorize here. The earlier
  "host restarts" framing was wrong.
- **Verdict:** science that ran was efficient (2 gates closed, 41 GPU-h saved by G1).
  New operating mode: Paul present to monitor/approve; work packaged as ONE
  pre-approved long-running batch script per day (a single approved nohup launch runs
  many hours with zero further prompts), not as many small tool calls.

## 2026-07-14 · A3 verdict: G1 FAIL (no rollout) — pairing is everything, radial fix is neutral
- **Goal:** decide G1: does the fixed radial-norm target loading improve relevancy vs the
  pre-fix 01 baselines (abC_20k, trained 2026-07-10)?
- **Command/config:** automation/eval_R_ab.sh — full_relevancy_eval, NF=60, both arms
  scored with 01_13B_v1; diagnostic: old arm scored with 02_block000_v1.
- **Result (obj acc / mIoU / mAUC-PR, per block):**
  - b000: old 66.2%/0.627/0.740 vs R 51.2%/0.606/0.678 → R WORSE (−15 pts)
  - b001: old 73.0%/0.669/0.744 vs R 76.7%/0.669/0.688 → R +3.7 pts, AUC slightly down
  - b002: R 54.5%/0.620/0.657 (no abC baseline exists to compare)
  - Diagnostic: old arm scored with WRONG decoder (v02) collapses to 6.3%/0.191 —
    decoder-target pairing dominates everything.
- **Interpretation:** (1) the 01 abC baselines were evidently built correctly paired with
  01_13B_v1 — they decode fine; the radial-load fix on top is NEUTRAL-to-noisy at leaf
  level (level norms matter for hierarchy levels, not leaf cosine direction). (2) The 03
  pilot's dramatic deltas (0% old arm, 2.5× IoU) were the PAIRING effect (03 old block was
  baked with 02_block000_v1), not the radial fix. (3) The committed unification/cache fixes
  are what prevent the catastrophic 0–6% mismatch cases — they stay, and correct pairing is
  mandatory for all future blocks (E track). 
- **Verdict:** G1 FAIL per pre-registered criterion (needed ≥+3 pts systematic, got mixed
  −15/+3.7). A4 mass-rollout CANCELLED — saves ~41 GPU-h; existing 01 baselines remain the
  served splats. GPU reallocates to C1 (sky ablation) + E track. R_20k runs kept on disk
  as the fixed-load reference arm.

## 2026-07-13 · A2 pilot verdict (03 block_000) + R-retrain relaunch (01 blocks)
- **Goal:** measure the fixed-load target rebuild (v1 embedder, high@d091981) vs the
  pre-fix baseline on block_000; then expand to the 01 survey.
- **Command/config:** regression_fixture.sh full w/ HIGH_EMBEDDER_CKPT=03_13B_v1;
  full_relevancy_eval.py both arms, scored with 03_13B_v1 (releval_old/new in scratchpad).
- **Result:** (1) photometric neutral: TRAIN psnr_fg 26.88 vs 26.78 (historical Jun-23 run)
  → regression PASS, fixture band recalibrated to 26.78±0.7 (old 23.6 band was an eval-FG
  figure from a different config). (2) Semantic: fixed-load arm 2.5× mean IoU (0.33 vs
  0.13) and AUC-PR (0.32 vs 0.13); tree 4 IoU 0.50 vs 0.11. BUT n=2 trees × 1 frame —
  thin GT on this block; and object-pointing 0.00% in BOTH arms (over 253k px) = suspected
  palette-id ↔ word-index mapping artifact in the eval, to investigate before A3. Old arm's
  0% also reflects decoder mismatch (its targets were baked with hardcoded 02_block000_v1).
- **Also:** discovered the other session's 13:24 R-retrain runs (01 blocks 000/001) crashed
  at launch on pre-d091981 code (HyperEmbedderConfig missing level_target_norms), 0 ckpts —
  quarantined; relaunched R-retrain on current code: 01/lio_row 000-002, recipe recovered
  from the crashed run's config.yml (antialiased+depth-sup 20k), automation/train_R_block.sh.
- **Verdict:** direction supports the fix; decision-grade numbers come from the 01 A/B
  (dense GT, established 76%/88.5% baseline) once the three R blocks land (~21:15).

## 2026-07-13 · Tassili POC Days 2–3: compaction sweep + fidelity gate 35/35 PASS
- **Goal:** shrink the 35-block serving payload without losing detail, with a
  render-diff gate as proof (POC plan: plans/tassili_serving_poc.md).
- **Command/config:** `compact_block_splats.py --min-alpha 0.02 --crop-margin 8`
  (system python3); `render_fidelity_gate.py --stride 20 --convention cv`
  (nerf_new pixi, gsplat 1.4). Gate = GT-delta: compact scene (full tier + 20 m
  trajectory-neighbour fulls + far tiers) vs baseline scene (original PLYs at the
  SAME residency), both judged against kf GT with fg-masks. Prune gate = prune-only
  vs GT delta.
- **Data:** 01_13B blocks_ns/lio_row, 35 blocks C_20k; kf_images + fg_masks GT.
- **Result:** 44.2M→34.6M gauss; 2.43 GiB PLY → 1.03 GiB full .splat + 190 MiB far
  tier (6.2M gauss). Gate 35/35 PASS: prune GTΔ ≤ +0.20 dB (median ~+0.02);
  compact GTΔ mean −0.04…−3.74 dB (BETTER than baseline — pruning removes the
  low-alpha haze that multi-resident original blocks overlay on each other);
  worst view +0.81 dB. Iterations that mattered: transforms.json is OpenCV c2w
  (GL flip → black frames); single-block gate unfairly billed crop for content
  neighbours provide (had to model residency); crop at margin 8 is a no-op —
  pruning is the whole win. Brotli on .splat: 95% — dead end, skip .br.
- **Serving:** server.py auto-serves compact/{full,far}.splat + manifest far_url
  and n_gauss fields; splat_loader.js rewritten two-tier (far always-resident,
  gauss-budget 6M residency, serialized scene mutations). Verified: /splats/005.splat
  31 MiB, /splats/005.far.splat 5.6 MiB, manifest 35/35 with counts.
- **Verdict:** keep — compaction validated and live on 8001; browser flythrough of
  the new loader is the remaining unknown (Paul tunnel session). Gate artefacts:
  per-block compact/gate.json + PNG dumps in block_005/.../compact/.

## 2026-07-13 · A1 verdict: graph trainer broken, monitor exonerated; A2 pivoted
- **Goal:** decide A1 after the v1-matched rerun also ended ~random (01: 2/289, 03: 3/272
  final object acc).
- **Command/config:** monitor_v1_check.py (scratchpad) — train.py's fixed monitor_hierarchy
  on the SAME HierarchyDataset for both ckpts.
- **Result:** 01_13B_v1 = **56.06%** object (162/289); 01_13B_v2R = 0.69% (2/289). Monitor
  is reliable; `train_hyperembedder_graph.py` does not reproduce v1 at v1's own hyperparams
  (v1 likely trained via the original train.py mask/CORAL path). v2R quarantined.
- **Verdict:** A1 FAILED, but it surfaced the better experiment: v1 decodes well under the
  config-aware loader (high@d091981), so A2 becomes "v1 + fixed loading targets" vs
  "pre-fix baseline blocks" — no new embedder needed. Pilot on 03 block_000 launched
  (doubles as full regression fixture after today's high commits), stale clip cache
  quarantined first. FOR PAUL: if a true embedder retrain is ever wanted, it needs the
  original train.py path; the graph-only shortcut is not currently viable.

## 2026-07-13 · A1 embedder retrain, attempt 1: FAILED (defaults ≠ v1 recipe)
- **Goal:** retrain 01/03 HyperEmbedders (v2R) with config-loaded level norms for the
  G1 A/B (autonomous-week Track A).
- **Command/config:** train_hyperembedder_graph.py, script DEFAULTS: temp 0.5, batch 16,
  400 epochs (log: scratchpad/a1_embedder_retrain.log, quarantined ckpts in _quarantine).
- **Result:** losses moved but object-accuracy monitor flat at exactly 1/N (1/289, 1/272)
  for ALL epochs, both surveys. Config diff vs v1 (config.json): v1 = temp 1.0, batch 50,
  1000 epochs; weights identical. So the graph trainer's defaults do NOT reproduce v1.
- **Verdict:** discard; rerun launched with v1-matched flags (--temperature 1.0
  --batch-size 50 --epochs 1000), nohup pid 665673, log a1_rerun_v1matched.log.
  Decision note for A2: if the monitor is STILL flat 1/N on the v1 recipe, treat the
  monitor as unreliable (v1 itself yielded 76% downstream relevancy) and gate A2 on a
  direct decode sanity check of the ckpt instead, not the in-training table.

## 2026-07-13 · Tassili serving POC Day 1: stage 01 lio_row + baseline
- **Goal:** serve the full 35-block 01 survey on the tassili viewer; capture the
  serving baseline before compaction/LOD work (week POC: optimise flythrough
  without sacrificing detail).
- **Command/config:** new `scripts/stage_splat_manifests.py` (--config lio_row
  --run splat_runs_C_20k, tree bboxes donated from lio_arc index.json);
  `build_survey_hierarchy.py --mode spatial` after symlinking C_20k exports into
  the expected `splats/splat_cropped_high_full_v16_unified.ply` layout; server
  restarted on 8001 with HIGH_DATA_ROOT=01_13B_Jackal HIGH_SPLAT_CONFIG=lio_row
  (was pointing at stale 02 defaults; log: aru_sil_core/outputs/splat_viewer_01_liorow.log).
- **Data:** 01_13B_Jackal blocks_ns/lio_row, 35 blocks, C_20k exports.
- **Result:** /scene/blocks ready 35/35, tree_to_block 289, hierarchy 48 nodes
  (12 spatial interior), /tassili/ 200. Baseline: 44.2M gaussians, 2.43 GiB PLY
  total; mean block 1.26M gauss / 71 MiB; localhost fetch 76 MB in 0.68 s.
  Outlier note: block bounding radii up to 102 m (block_034; 031=81 m) vs ~50 m
  row length → floaters present; skews centre-distance LOD.
- **Verdict:** keep — baseline established; next: compaction (prune/crop/convert)
  then two-tier LOD vs Spark 2.0 spike. Browser-side FPS baseline pending Paul's
  tunnel session.

## 2026-07-13 · Day 0: autonomous-week setup
- **Goal:** stand up the self-driving experiment week (2026-07-14 → 20) per
  plans/autonomous_week_2026-07-14.md.
- **Command/config:** exp branches `exp/2026-07-14-week` cut in high + aru_sil_core/src;
  automation/{queue.json, SUPERVISOR.md, regression_fixture.sh}; in-session cron
  supervisor every 3 h (job 7728324d, 7-day expiry); quarantine dir created.
- **Result:** fixture smoke (500 it) launched on exp branch — cache fingerprint dir,
  67/67 semantic PNGs, and non-collapsed level_target_norms [11.1, 8.325, 0, 2.775] all
  confirmed in the log (today's three fixes engaged). GPU: V100-32GB idle otherwise.
- **Constraints (Paul):** NO pushes all week — everything stays local incl. the
  review-pending commits; commit-on-green to exp branches authorized; nothing deleted
  (quarantine only). Supervisor session must stay alive (do not archive this session).
- **Verdict:** keep — queue starts with A1 (embedder R-retrain) once smoke passes.

## 2026-07-13 · Closed-form palette validation + pre-leave commit sweep
- **Goal:** validate the rewritten `save_filtered_semantic_pngs.py` (palette.json built
  closed-form from `GetMarkerColor`, probe + 902 magic default dropped) against a
  known-good probed block before relying on it for multi-block runs.
- **Command/config:** `save_filtered_semantic_pngs.py --block-dir 03_13B/row1_halfB
  --out-dir <scratch>/test_sem --verify` (nerf_new pixi). Static check: closed form vs
  all existing palette.json files (01 lio_row blocks + 03 halves).
- **Data:** 03_13B_Jackal row1_halfB (98 frames, 272 markers); 01_13B lio_row palettes.
- **Result:** 272/272 closed-form pairs; old probed palette a consistent subset (22→272);
  all 98 PNGs byte-identical; `--verify` frame-probe agrees; statically 5,867/5,867
  entries match across two surveys (gids to 680, BGR order). Also landed: identity-blind
  semantic-mask cache fix in `high` (keyed by data path + PNG/palette fingerprint —
  removes both the cross-survey block_000 collision and the nuke-before-ns-train rule).
- **Commits:** aru_sil_core/src `c50c995f` (palette), `577d444c` (4D association scripts
  promoted from skill scratchpad); high `1fbe48e` (cache fix), `d091981` (post-triage
  encoder/radial follow-ups — NB message may overlap with 8945f3f's earlier
  HIGH_EMBEDDER_CKPT landing, amend if wrong); ujamaa `0d098f8` (SANKOFA_SPEC + POC),
  `9014e16` (Bateleur wiring + splat_viewer). NOT pushed — push after review.
- **Verdict:** keep — both silent-corruption pitfalls (stale cache, 902 palette cap) are
  structurally fixed; multi-block/away-week runs no longer need manual cache hygiene.

## 2026-07-13 · Git triage (Phase 0)
- **Goal:** capture all uncommitted pipeline work before the 5-month campaign (June
  post-mortem: uncommitted work = weeks lost).
- **Result:** `high/` 4 commits (LiDAR depth-sup loss + bilateral fix `cc59198`,
  HIGH_EMBEDDER_CKPT unification `8945f3f`, paper_figs, gitignore). `aru_sil_core/src`
  6 commits on `splat/walkthrough` (multi-rig l2c `ba2b3752`, ts-match idx fix `3027b664`,
  WGS84 ingest `8067bc1a`, viewer `35ebb8c7`, hierarchy-monitor fix `8f93f3c2`, eval/row
  tooling `3cc0a0f5`) + `load_hyper_embedder` config fix. Both tagged
  `known-good/2026-07-13`; both trees clean. `highnerf/` is an empty dir.
- **Verdict:** keep. Both repos pushed same day: `aru_sil_core/src` → splat/walkthrough on
  African-Robotics-Unit/aru_sil_core; `high/` → main on African-Robotics-Unit/aru_sil_high
  (new repo). Tags pushed too — off-machine backup complete.

## 2026-07-13 · Notebook opened; 5-month campaign starts
- **Goal:** establish the research operating system for the UJAMAA demo + release push.
- **Result:** UJAMAA_ROADMAP.md created (finish line: public demo + code release, ~Dec 2026).
- **Verdict:** keep — all future runs get logged here. Backfill of pre-July runs lives in
  memory files (high_01_13B_row_survey, high_4d_association_state), not here.

## 2026-07-23 — semantic point cloud (SAM3-masked LiDAR), 04_13D
**What:** per-keyframe LiDAR projected into images; kept only points landing on SAM3
tree-mask pixels (union per frame), 10 m depth clip (matches clustering's max_det_dist);
accumulated at 0.1 m. Two-stage run: mask decode in sam3 env (`/tmp/sem_stageA.py` →
2,080 frames), project+filter in nerf_new env (`/tmp/sem_stageB.py`).
**Artifacts:** `sankofa_substrate/semantic_cloud_04_topdown_10m.npz` (142,303 cells;
unclipped variant `semantic_cloud_04_topdown.npz`, 222,643 cells — masks over far trees
pull LiDAR to ±100 m, don't use for clustering comparisons);
fig `figs/semantic_cloud_04_topdown.png`.
**Finding (revises the audit's root cause):** 110/112 census canopy peaks have ≥10
strong (count≥5) semantic cells within 1 m — the SAM3-gated input covers essentially
EVERY tree. So the 28 registry-missing trees are lost DOWNSTREAM in the voxel-CC
clustering stage (merging into multi-tree components / min_voxels / bookkeeping), NOT
by SAM3 mask misses as I previously suggested. Individual canopies are cleanly resolved
in the semantic cloud; visible streak artifacts where masks bleed onto trellis/ground.
**Implication (Paul's call):** re-clustering the EXISTING semantic cloud (e.g.
peak/watershed-seeded with the 5.9 m pitch prior) could recover the full census without
re-running SAM3 — the gated input is not the bottleneck.
**Addendum (ground margin):** Paul caught that the registry pipeline removes ground
BEFORE clustering — `cluster_tree_instances.py --ground-margin 0.6` (default; recorded
in 04's global_ids.json config): drop points not ≥0.6 m above the nearest trajectory
pose height (y-down; kills alley-floor two-pass bridges + trajectory phantoms). Re-ran
stage B with the identical cut → `semantic_cloud_04_topdown_10m_ground.npz`
(101,074 cells; streak artifacts gone). Census check on this EXACT registry input:
**109/112 peaks still covered** — root-cause revision stands: the losses are in the
voxel-CC step, not SAM3, and not the ground filter. Figure regenerated
(`figs/semantic_cloud_04_topdown.png`).
**Addendum 2 (no pitch prior in 13D):** Paul: the ~5.9 m pitch is 13B-only; 13D (04/05)
trees are much closer — no pitch prior there. Measured from the 04 census peaks:
NN spacing median 4.18 m (p10 3.08, p90 5.04), irregular; 29/112 pairs sit AT the
census's 3.0 m NMS floor → suppression was active → **112 is a LOWER bound** on 04's
tree count. Retract the "seed with 5.9 m pitch prior" suggestion for 04/05; any
re-clustering there must be spacing-agnostic (data-driven seeds, no fixed radius).

## 2026-07-23 — no-NMS census + watershed recluster, 04_13D
**What (Paul's instruction):** census peaks WITHOUT the 3.0 m greedy NMS (residual
scale = only the 2.0 m local-max window), then watershed (`ndimage.watershed_ift`)
over the SEMANTIC cloud (10 m + 0.6 m ground margin = exact registry input) seeded
from those peaks. Script `/tmp/census_nonms_recluster.py`.
**Numbers:** 150 peaks (vs 112 with NMS, 85 registry) → 149 with semantic support →
**135 reclustered trees** (≥25 cells). vs registry: 106 within 3 m, 29 with no
registry neighbour, 26 registry components split into ≥2 new clusters. New-cluster
NN spacing median 2.92 m (p10 1.54 m).
**Artifacts:** `sankofa_substrate/recluster_04_nonms_v1.npz` (+ `_cells.npz` per-cell
labels), fig `figs/recluster_04_nonms.png`.
**Caveats (unresolved, Paul to judge):** p10 spacing 1.54 m suggests SOME seeds are
double-peaks on one canopy (over-segmentation risk); a few straight-line watershed
boundaries (uint8 plateau artifact); grey cells = sub-25-cell specks. 135 is an upper
candidate set the same way 85 was a lower one — truth likely between.
**Addendum (v2, full coverage):** Paul asked why some semantic points had no instance.
Diagnosis on v1: 16.2% of cells (16,401) were eaten by MY background watershed marker
(flooded into any canopy without a seed of its own); 0.1% specks; the seedless canopies
concentrate in the low-height band z≈32–38 (peaks under the 0.9 m smoothed threshold)
plus far-field fragments outside the census's 9 m trajectory bubble. v2 fix: outside
impassable (no competing background), seedless components ≥25 cells get a fallback seed
at their own height max (101 fallback seeds), stragglers to nearest instance → every
semantic cell now belongs to an instance. Result: **231 instances** — over-segmented
(NN median 1.77 m, p10 0.49 m; the low band shatters into scraps; 44 with no registry
neighbour incl. far-field fragments). Bracket for 04: registry 85 (under) < census 112
(lower bound) < v1 135 < v2 231 (over). Artifacts: recluster_04_nonms_v2*.npz,
figs/recluster_04_nonms_v2.png. Open: instance criterion for low-canopy scraps —
Paul to decide (merge pass / height-adaptive peaks / scrap-to-neighbour attachment).
**v4 (census-NMS seeded — Paul's spec):** seeds = the census peaks AS RUN (2 m
local-max, 0.9 m height, 3.0 m greedy NMS, 9 m bubble; 110 after plateau-collapse,
109 with semantic support) → watershed over the semantic cloud, no background flood,
no invented instances; leftover cells attach to nearest census tree. Result:
**103 instances, 100% semantic-cell coverage**; NN spacing median 4.40 m (p10 2.93,
consistent with the NMS floor); vs registry(85): 81 matched <3 m, 9 registry
components split, 22 instances unmatched (mostly headland/far-edge). Artifacts:
recluster_04_nms_v4.npz + _cells.npz; figs/recluster_04_nms_v4.png. v3 valley-merge
recorded as a NEGATIVE result (valley-depth distribution structureless; count slides
103→20 with threshold — top-height carries no boundary signal for touching trees).
**Fruit remap onto v4 (2026-07-23):** every 04 fruit view placed by ITS OWN mask
(per-view RLE matched by gid+area, LiDAR in-mask median with the same 10 m + 0.6 m
ground cuts, `/tmp/fruit_remap_A.py`+`_B.py`) then assigned to the v4 instance at that
position. 3,616/3,695 views placed (79 <5 lidar pts; 1 view >1 m from any v4 cell).
Result: 101/103 instances observed, **20 fruiting** (vs 17 on old components — 3 old
fruiting components split their fruit across >=2 v4 trees), max 18/view unchanged.
Artifacts: sankofa_substrate/fruit_views_v4.npz (per-view pos+label),
fruit_04_v4.npz (per-instance), figs/fruit_04_v4_topdown.png.
**Zero-view autopsy (Paul's "missing trick" hunch — confirmed):** rendered a zero-fruit
view of the max-18 tree (kf 536 vs fired kf 543, 7 kf apart): the tree mask is a thin
sliver at the frame edge — the tree was ENTERING the frame; the crop SAM3 scored is a
~150 px-wide strip. Quantified across all 3,695 views: fired-rate rises monotonically
with tree-mask area — Q1 (slivers) 0.2%, Q2 0.6%, Q3 3.1%, Q4 4.7%, top-10% 5.4%;
views >= Q3 area (25% of views) carry 59% of all fruit detections. So per-view zero
counts are dominated by GEOMETRY (partial views), not detector failure; any per-view
rate stat (median fruit, fired %) is diluted by slivers. Candidate fix (Paul to
decide): gate/weight per-tree stats by tree_mask_px (e.g. only score views >= ~125k px)
— tree_mask_px is already recorded per view, zero recompute needed. Secondary
observation from the same frame: substantial FALLEN fruit on the ground is visible and
currently ignored by design (parent-mask rejection excludes it).
Fig: figs/fruit_zero_vs_fired_view.png.
**Tree-31 two-pass autopsy + luminance stats:** the kf-394 backlit tree (old gid 31)
has NO fired view in any pass — seen on 3 passes (kf ~390s, ~1060s, ~3070s, both drive
directions), all views zero, best masks 290k/152k px, in-mask luminance 0.27/0.22.
Fired-vs-zero luminance across the survey: fired median 0.31 (p10 0.19) vs zero median
0.20 — a real shift, but 36% of fired views are at/below tree-31's brightness, so
darkness alone doesn't preclude firing → this tree is most plausibly GENUINELY sparse/
bare on its visible sides, distinct from the sliver-geometry and backlit-exposure zero
mechanisms. Fig figs/tree31_two_passes.png.
**DEFECT found via the same query — v4 instance 105 is a placement sink:** 286 views
from ~40 old gids spanning the entire survey (kf 54→3518) map to it (centroid 19.7,33.3
— the low-canopy band). Cause: bad/backlit masks with see-through include background
LiDAR, dragging the per-view median into the open field, where nearest-cell attachment
hands them to the big low-band instance. Only 4 fired views (max 4) landed there, so the
fruit map is mostly unaffected, but instance 105's n_views/fired stats are garbage and
any per-view rate for it is meaningless. Candidate guards (Paul to decide): require
median-to-mask-bearing consistency, cap median distance to the view's own tree bbox
depth, or drop views whose in-mask LiDAR spread exceeds a canopy scale.
**05 fruit pass stats (vs 04):** 82 trees / 3,277 views: fired 3.6% (04: 2.2%),
fruiting trees 22/82 = 27% (04: 17/85 = 20%), detections 588 (04: 382), max/view 18
(same). Area-quartile fired rates 05 vs 04: Q1 0.7/0.2, Q2 3.2/0.6, Q3 5.3/3.1,
Q4 5.4/5.0 — the gain concentrates in MID-SIZE masks (Q2-Q3), and detections are less
top-heavy (top quartile carries 38% of detections vs 59% in 04) → consistent with more
favourable lighting/geometry rather than more fruit. Same-block surveys: a 04<->05
association on v4-style instances would turn this into a per-tree repeatability check.

## 2026-07-23 — YOLO-World zero-shot validation (CPU): FAIL on the hard cases
**Setup:** ~/venvs/yolow2 (virtualenv, CPU torch + ultralytics; python3-venv unavailable
without sudo). Prompt "orange fruit", conf 0.08, models yolov8s/l-worldv2 @640+1280.
Cases: obj-119 crops (visible oranges, SAM3=0), frames 543/536/394/1064/1065, 20
largest-area SAM3-zero views. Scripts /tmp/yolow_{validate,sweep_04,place_04}.py +
chain.sh — sweep/placement stages written and ready but STOPPED after validation.
**Results:** obj-119 crops: 0 dets (both models, both sizes) — fails the exact
motivating case. frame 543 (SAM3 best 18): l@1280=16, s@1280=8, both @640≈0.
frame 536 (SAM3 9): l@1280=4. Backlit/cross-alley frames 394/1064/1065: 0 everywhere.
Zero-Q4 views: l fires 2/20. 640 input ~useless → resolution-bound.
**Verdict:** zero-shot open-vocab detection ≈ SAM3 on easy near-field, recovers NOTHING
on the small/soft cross-alley fruit. The s-model full sweep would have produced a
strictly weaker map — killed it (3-4 h CPU saved). Remaining uplift paths (Paul):
(a) fine-tune a small-object detector on ~100 labelled crops from our own fired views,
(b) l-model@1280 sweep for ground-fruit layer only, (c) accept best-view semantics +
area/luminance gates. Validation fig: figs/yolow_validation.png.

## 2026-07-24 — TRUE 5-level hierarchy (section→SR→row→tree→fruit) VALIDATED, 04_13D
**Paul's correction:** no level-shifting — trees stay objects; fruit is a NEW level
below them. Implemented additively: `H["fruits"]` in the hierarchy json; graph-dataset
subclass prepends fruit nodes (fruits-first ordering, node_type 4, FRUIT_WORDS vocab);
`Fruit (retrieval)` metric added to the monitor; `--keep-super-row` retains the SR
level. Model needed NO changes (verify_5level_hierarchy.py already proved depth-
agnosticism). Files: train_hyperembedder_graph.py, word_utils.py, train.py (aru,
uncommitted); hierarchy scene_graph_v4/marker_hierarchy_fruit5.json (103 trees, 20
fruit nodes, 11 planting rows [8 in-field + N/S headland + strips], 2 SRs, 1 section).
**Trained 04_13D_v2F5** (c20cos20 template, 1500ep): **Fruit→tree retrieval 100%
(20/20); tree→planting-row retrieval 99.03% (102/103)**; Section walk 100%. SR walk
77.5% (walk metric — layout-dependent, not primary). Ckpt
/home/paperspace/data/high/nerf/04_13D_v2F5/ckpts/model_best.pth.
**Caveats:** fruit level is detection-floor (20 = fired trees; known misses + 05 saw
22); hierarchy tree 99 = v4 label 105 placement SINK — its fruit node is an artifact;
interim level-shifted run 04_13D_v1F (98.37%) retired, design rejected by Paul.
**Awaiting Paul:** drop sink node + rebuild; 04↔05 association to union fruit
evidence; embedder pairing (which splats, if any, decode against v2F5).
**Interpolation-path retrieval on v2F5 (Paul's question — geodesic across layers):**
probe /tmp/interp_probe_v2F5.py using the canonical L.get_interpolated_hyperbolic_
features (12 steps, leaf→root): interp-path = flat at EVERY level and depth —
fruit→tree 100%, fruit→planting-row 100%, fruit→super-row 100% (20 each);
tree→planting-row 99.03%, tree→super-row 99.03% (103). Same equivalence previously
shown on 03_13B_v2G. (First probe printed fruit→row 10% — my invalid fallback that
scaled hyperboloid coords linearly instead of walking the geodesic; discard.)

## 2026-07-24 — fruit-pixel GT + fruit-supervised splat (block_001 F2)
**Paul's call:** no fruit GT masks existed (fruit pass discarded geometry) so fruit
localization was unverifiable; fruit was never directly supervised in splats (tree-level
pixel targets only; fruit entered via embedder geometry alone). Chain shipped:
Stage P re-placed 186 block_001 tree masks -> v4 labels; Stage H re-ran SAM3 fruit
prompting on fruiting-tree crops SAVING masks: **166 fruit masks painted over 24
frames**, palette +20 fruit ids (200+fruit_idx, collision-checked).
`high_dataloader.py` gained the id>=200 -> FRUIT_WORDS branch (targets = fruit-NODE
embeddings; pairing preserved; note: targets encoded node_types=0 — inert under
no-level-norms template, needs a fruit branch if radial norms ever return).
F2 retrain (splat_runs_F2) running; eval upgraded: fruit-pixel GT split from tree GT
(fruit pixels count for both levels), new FRUIT-PIXEL POINTING metric (true fruit
pixel -> argmax over 20 fruit anchors). r6 blocks 004/005 stopped pending (north half,
tree 32 only) — GPU released to F2 then weekend E-03 handoff.
**Also today:** relevancy A/B (block_001 F1): interpolated > flat (mean IoU 0.172 vs
0.138); negatives = modest contrast gain (Paul's no-negatives ranking-equivalent);
object pointing 2.3% via WALK decode — instrument suspect (same artifact as graph walk
metric); retrieval-pointing variant is the pending fair measurement. Viewer defect
FLAGGED: clip_encoder.get_relevancy has steps=1 hard-coded (interpolation disabled in
served relevancy) — one-line fix awaiting Paul.

## 2026-07-25 — CEILING DIAGNOSTIC (Paul's design): scoring path exonerated, field convicted
**Method:** --gt-features eval — every painted pixel gets its EXACT supervision target
(log_map0(v2F5.encode(CLIP(word)))), identical metrics, no rendering. Ceiling vs
rendered (block_001 F2, fruit-first 30 frames):
object pointing 100.0% vs 3.6%; row 100.0% vs 39.7%; FRUIT-PIXEL pointing 100.0% vs
3.5% (chance 5%); tree relevancy IoU 1.000 vs 0.177.
**Conclusion 1:** walk-decode/anchors/scoring are PERFECT on perfect features — the
graph-side walk-metric caveat does NOT transfer; the rendered 32-d field is the sole
bottleneck (training-side: feature-loss weight / schedule / capacity — the fg
under-fit headroom issue). Fruit supervision (166 masks) did not fix it because
nothing renders faithfully yet.
**Conclusion 2 (Paul's negatives instinct proven):** fruit relevancy CEILING is
bimodal — trees 73/88 IoU 0.89/0.67 but 43/84 ~0.00 on perfect features: the
softmax-vs-negatives formula is fragile to vocab luck (decoded "saffron"/"nectar"
class words land nearer LERF negatives than their own anchor), while negatives-free
RELATIVE scoring (pointing among anchors) is 100% on the same pixels. Serving
formula should be relative/negatives-free (or negatives-off per query type).
**Evidence files:** /tmp/f2_relevancy_gtceil, f2_eval{,2,3}.log; eval script
/tmp/eval_r6_relevancy.py (--gt-features). Next decisions (Paul): raise
high-feature loss weight + retrain block_001 (GPU currently E-03 weekend batch);
adopt negatives-free serving formula + steps=4 fix in clip_encoder.

## 2026-07-25 — F3 (v15 background fix): magnitude CURED, discrimination is next
**Fix applied:** high_dataloader.py background (0,0,0) latent = out-of-scene phrase
("empty gravel background") instead of zeros (v15 regime); 16 stale colour-LUT caches
invalidated; block_001 retrained (F3, rc=0).
**Autopsy kf543:** rendered |feat| median 0.004 -> **3.006** (target 4.18; 750x
recovery), cos(rendered, target) median **0.632**, 100% painted px >0.5. BUT
background norm identical (3.007) and pointing DROPPED: object 0.9%, row 15.0%,
fruit-pixel 3.2%. Diagnosis: the field no longer collapses to zero — it collapses to
a GLOBAL MEAN embedding (~cos 0.6 from every correlated CLIP-noun target). Each
gaussian's gradient mixes conflicting ray targets (bg-dominated) and the geodesic
loss is mean-seeking with no separation term -> equilibrium = blended average;
argmax then picks one arbitrary anchor everywhere.
**Implication for 13B E-track:** all E-blocks trained with the bg-ZERO regime ->
their feature fields are F2-style dead. RGB/geometry fine; features repairable
post-hoc via freeze_geometry feature-only passes on existing ckpts (flag exists for
exactly this).
**Candidate next steps (Paul):** (a) down-weight bg term (Paul's instinct) so local
tree targets dominate; (b) feature-lr/schedule bump + longer training; (c) add a
separation/contrastive term to high_loss; (d) freeze_geometry feature-only repair
recipe (fast A/B: ~10 min/run on frozen geometry). E-03 weekend batch left RUNNING
(block_036+): geometry not wasted under any option.
**GEODESIC-LOSS MAP (Paul's ask) — my "global mean" story was WRONG.** Per-pixel
geodesic distance, kf543 F3, using safe_geodesic_loss maths (note: loss uses
curv=1.0 default while embedder curv=1.311 — separate issue, logged):
  painted (tree/fruit) px: median **5.668**   background px: median **0.018**
  nearest-target-in-32d (no decode): painted **0.0%** correct, background **99.9%**
  margin d(wrong)-d(own): median **-5.64**, 0% of painted px favour their own target
  most-predicted id on painted px: **-1 (the BACKGROUND word) = 82,629 / 82,840**
**Mechanism (not mean-collapse — background CAPTURE):** the field learned the
background word essentially everywhere, including on canopy. Each gaussian is
supervised across all frames it appears in; SAM3 paints only ~2 masks/frame on
87/124 frames, so a tree's own gaussians receive the BACKGROUND target in the
majority of their observations. The v15 fix made this worse for discrimination: a
strong real bg embedding wins outright where zeros only pulled weakly to origin.
**=> Paul's instinct (ignore background) is correct FOR THIS DATA REGIME** — sparse,
incomplete instance masks — which differs from the regime the v14 revert was made in.
Recommended next: mask the geodesic loss to painted pixels (or bg weight <=0.05) and
retrain block_001 (F4); densification concern is testable by comparing gauss count
vs F3. Fig: figs/geodesic_loss_kf543_F3.png.
**SAM3 confidence_threshold=0.5 was throttling ALL labels (Paul's question).**
Sam3Processor defaults to conf 0.5 (out_probs = query prob x presence); no size cap.
Sweep on block_001 frames: kf489 3→7→24→97 masks at 0.5/0.3/0.15/0.05; kf500 (a
"SAM3 saw nothing" frame) 0→7→29→107. At 0.15+ canopies fragment; at 0.05 ground/sky
get masked. **0.3 chosen.** Block_001 re-mask at 0.3: **973 masks over 124/124 frames
(was 314 over 87)** — the 37 no-mask frames and 15 headland all-dropped frames are
eliminated as a category.
**FRUIT sweep = negative result:** kf543 (already firing) 21→29→50 masks at
0.5/0.3/0.15 — real gain where fruit is detectable; but the obj-119 crop with
plainly visible oranges and the backlit kf394 tree return **0 masks at EVERY
threshold down to 0.05**. Confidence was not the cause of the hard fruit misses;
the detector-uplift parking stands. Take 0.3 for fruit anyway (+~40% on firing views).
Figs: sam3_conf_sweep.png, fruit_conf_sweep.png.

## 2026-07-26 — F4 (conf-0.3 labels + v16 masked loss): BIG improvement, not solved
**Inputs changed vs F3:** SAM3 conf 0.5→0.3 (block_001: 973 tree masks/124 frames,
606 placed, 604 painted/113 frames; fruit re-detect 650 masks/42 frames) AND v16
masked geodesic loss (unlabelled px = zero sentinel, excluded; high_loss_bg_weight
knob added, default 0). Labelled px in first batch: **20330/57600 = 35%** (was ~15%).
**Results (F3 → F4):**
  object pointing        0.9%  → **17.2%**   (chance ~1%)
  planting-row pointing  15.0% → **89.9%**   (13B E-track benchmark: 88.5%)
  fruit-pixel pointing   3.2%  → **23.0%**   (chance 5%)
  mean tree relevancy IoU 0.151 → **0.230**
  geodesic loss (canopy) 5.668 → **3.197**;  background 0.018 → 2.984
  nearest-target-in-32d (painted) 0.0% → 4.5%; bg→bg-word 99.9% → 0.0%
**Reading:** background capture is GONE (bg no longer wins anywhere) and row-level
identity now MATCHES the 13B benchmark. Object-level is 17× better but still far from
the 100% ceiling: nearest-target-in-32d stays low (4.5%) and predictions concentrate on
a few ids (88, 96, 99) — the field now separates ROWS but not individual trees; residual
per-gaussian target conflict across frames (a tree gaussian still sees several different
tree ids as it moves through masks) is the leading hypothesis.
**Next candidates (Paul):** (a) longer/higher-lr feature schedule now that gradients are
clean; (b) high_loss_bg_weight small (>0) A/B; (c) per-gaussian target consistency —
weight by mask agreement or drop conflicted gaussians; (d) fix loss/embedder curvature
mismatch (loss uses curv=1.0, embedder 1.311). Artifacts: splat_runs_F4,
/tmp/f4_relevancy, figs/geodesic_loss_kf543_F4.png.
**Why the geodesic loss does NOT go to zero (Paul's question) — established:**
Verified three ways on the F4 checkpoint (manual calc, model.get_loss_dict, and over
all 123 frames): actual loss on SUPERVISED pixels = **3.6–3.7 raw (0.37 weighted)**,
median 3.52/frame, min 1.23. Checkpoint params load identically to live (norm median
3.29); dataloader targets verified identical to independently rebuilt CLIP targets
(cosine 1.0000).
**Mechanism:** rendered features on painted px have norm 4.199 (target 4.178) and
**cos 0.989** — i.e. an ~8.5° angular error. On the hyperboloid at radius 4.2,
sinh(4.2)≈33 AMPLIFIES angular error: cos 0.989 → geodesic 3.2. To reach loss≈0 the
field would need cos≈0.99999. Two structural reasons it cannot: (1) a rendered pixel
feature is an alpha-composite Σw_i f_i of many gaussians — a Euclidean average that
cannot equal a fixed high-radius target except where one gaussian dominates (this is
why the only converged region in figs/geodesic_loss_kf543_F4.png is one big
well-isolated canopy, and why mask edges peak at 5–7); (2) fruit targets sit at radius
7.14 vs trees 4.18 — even further out, and they are the bright dots inside the canopy
in that figure (unfit).
**Implication:** the metric is radius-amplified, so "loss ~3" ≠ "field is wrong" —
cos 0.989 is close. LERF avoids this by normalising embeddings and using cosine.
Candidate fix: angular/cosine loss on the tangent vector (or normalise before the
geodesic) so supervision is not stiffened by target radius.
**UNRESOLVED:** the training log for the same run reports 0.012–0.021 weighted
(raw 0.12–0.21) at the end — 18× lower than the checkpoint evaluates to. Params and
targets are identical, so the remaining suspect is train-mode vs eval-mode RENDER.
Could not complete that A/B: a second CUDA context now fails with an NVML
driver/library version mismatch (host libs updated under the running driver; needs a
reboot when the GPU queue is clear).
**RESOLVED (partly) — training log vs eval discrepancy.** Instrumented the REAL
training loop (env HIGH_LOSS_DEBUG=1, new per-step print in high_model.get_loss_dict)
and resumed F4 from its own checkpoint:
  step 20000: labelled=324856  mean_geo_labelled=**3.7331**  loss=**0.37331**
  step 20001: labelled=354958  mean_geo_labelled=4.5334      loss=0.45334
i.e. **the training loop itself reproduces the eval number exactly (0.373)**. The
tensorboard series for the same run (last-1k mean 0.01725) is therefore the ANOMALY,
not the eval. Cross-check that isolates it: of all loss terms, only `scale_reg`
(parameters only, no render) matches between log and eval (0.00144 vs 0.00150);
every render-vs-target term is 3–21× higher at eval. Two contributors identified and
fixed in the probe (eval had NOT exported LIDAR_DEPTH_NPZ so lidar_depth used a stale
03_13B npz; eval uses fixed background where training used random) — but high_loss
uses neither, and it still reproduces at 0.373 inside training.
**Ruled out:** stale/mismatched target cache (both used outputs/block_001_c29fea089971,
rebuilt 17:04, same v2F5 embedder, same 123 PNGs); checkpoint load (ckpt params
identical to live, norm median 3.29); camera poses (camera_optimizer mode='off');
render resolution (full-res == half-res, 3.199 vs 3.197); numerical precision —
measured fp64 == fp32 exactly at cos 0.989→3.274; (bf16 WOULD destroy it: inner
product quantises to ±8 and clamps to dist 0, but splatfacto runs mixed_precision off).
**Operational conclusion:** the eval numbers are trustworthy; the tensorboard
`Train Loss Dict/high_loss` curve UNDER-REPORTS by ~20× and must not be used to judge
convergence (this also invalidates the earlier "F4 still descending 43%" reading, and
the same pattern appears in F2/F3). True state of the field: cos 0.989 to target,
geodesic ~3.7, tree pointing 17.2%, row pointing 89.9%.
**Still unexplained:** why the logged scalar differs from the value get_loss_dict
returns in the same loop. **BLOCKER:** further GPU forensics stopped — new CUDA
processes now die with NVML_SUCCESS assert (host NVIDIA libs updated under the running
driver); needs a reboot when the GPU queue is clear. Debug hook left in place behind
env HIGH_LOSS_DEBUG (no cost when unset).

## 2026-07-27 — loss-discrepancy forensics: ONE REAL BUG FOUND (my eval scripts), F4 residual still open
**GPU is healthy again** (nvidia-smi still prints the driver/library mismatch, but CUDA
compute, allocator churn, large allocs and full ns-train all work — no reboot needed).
**BUG FOUND — eval scripts did not pin HIGH_EMBEDDER_CKPT.** `eval_setup` rebuilds the
datamanager→dataloader, which builds CLIP targets with the DEFAULT embedder
(01_13B_v1, level_target_norms=[11.1, 8.325, 0, 2.775]) instead of 04_13D_v2F5
(norms []). Targets then sit at radius 11.1 instead of ~4.2 and every geodesic
distance inflates. Demonstrated on a fresh 1500-step run:
   unpinned eval of step-1500 ckpt : 10.665 raw   (≈ the 11.1 target radius)
   PINNED   eval of step-1500 ckpt :  1.832 raw  vs TRAINING LOGGED 1.788  -> MATCH
So: logging path is faithful (logged == in-loop get_loss_dict, 1.00x at every step),
checkpoints ARE faithful, and ckpt==log when the embedder is pinned. This is the same
class as the documented "targets built with X, queried with Y" pairing bug — it bit in
the MEASUREMENT scripts, not the pipeline. Fix: always export HIGH_EMBEDDER_CKPT for
any script that calls eval_setup.
**Still unexplained (F4 only):** with the embedder pinned, F4's final ckpt measures
3.63 pixel-weighted (per-frame median 3.52, min 1.23, 111/123 frames labelled) while
its own training logged 0.0172 (raw 0.17) at step 19990. The fresh run shows no such
gap, so this is specific to that run's ending, not a systemic ckpt/logging fault.
Leading hypothesis (untested): the logged scalar is an UNWEIGHTED per-frame mean, and
frames vary enormously in labelled-pixel count (measured 313 -> 20,871 px in the fresh
run); low-count frames are interior-only and well fit, so a late-training window
dominated by them logs ~0.01 while the pixel-weighted truth is ~3.6. Test: log
labelled-count alongside the loss for a full run and correlate.
**Unaffected by all of this:** pointing/relevancy numbers (tree 17.2%, row 89.9%,
fruit-pixel 23.0%) — those build anchors from CLIP words with an explicitly-passed
hyper ckpt and never touch the dataloader target table. Same for the cos/norm autopsy
(feat_undertrain builds its own v2F5 targets: norms 4.199 vs 4.178, cos 0.989).

## 2026-07-27 — RESOLVED: opacity resets corrupt checkpoints saved on the boundary
**Method:** 20k ladder run (block_001), ckpt every 2000 with save-only-latest OFF,
per-step in-loop loss captured; every ckpt then evaluated against its own logged loss
(embedder pinned). `/tmp/ladder_compare.py`.
**Result — 8/10 checkpoints TRACK, 2 are wildly off:**
  step  2000 log 1.629 ckpt 1.354 (0.83x)   4000 1.046/1.071 (1.02x)
  step  6000 log 0.786 ckpt **4.004 (5.10x)**
  step  8000 0.480/0.784 (1.63x)           10000 0.438/0.420 (0.96x)
  step 12000 log 0.500 ckpt **3.744 (7.49x)**
  step 14000 0.343/0.601 (1.75x)           16000 0.272/0.229 (0.84x)
  step 18000 0.248/0.359 (1.45x)           20000 0.245/0.297 (1.21x)
**Cause — OPACITY RESET.** reset_alpha_every=30 x refine_every=100 => every 3000
steps. Measured loss median 60 steps before vs after each boundary:
  3000 1.92x | 6000 3.17x | 9000 3.94x | 12000 3.59x | 15000 1.05x | 18000 0.76x
Spikes occur at every multiple of 3000 UNTIL refinement stops at 15000, then vanish.
Resetting opacities changes alpha-compositing, so the rendered feature field is
wrecked and re-converges over the following few hundred steps. A checkpoint written
ON that boundary stores the wrecked transient — hence 6000 and 12000.
**Conclusions:** (1) logging path, checkpoint save/load, densification handling
(optimizer param IS dict param through every refine), precision, resolution, gradient
scale and frame sampling are ALL fine — each verified. (2) The instruments never
disagreed; only the STATE at save time did. (3) Exported splat feature quality is
save-timing dependent.
**ACTIONS (Paul to approve):** (a) never save on a multiple of reset_alpha period —
offset steps_per_save, or (b) verify feature loss before export and re-save if the
ckpt is >2x its logged loss, or (c) set reset_alpha_every large enough that no reset
occurs (test RGB impact first).
**STILL OUTLYING:** F4's own final ckpt (step 19999, 21x) is NOT on a reset boundary
(resets stop at 15000) — same class of bad save, trigger unidentified. The ladder's
equivalent ckpt (step 20000) tracks at 1.21x, so this is not systematic.

## 2026-07-27 — HEADLINE: clean 20k checkpoint => the hierarchy WORKS in the splat
Ran the identical pointing/relevancy eval on the LADDER run's clean 20k checkpoint
(feature loss 0.297 raw, vs F4's corrupted 3.73) — same block, same config, same
20k steps, embedder pinned:
                        F4 (bad ckpt)   CLEAN 20k ckpt
  tree pointing              17.2%   ->   **95.8%**
  planting-row pointing      89.9%   ->   **99.1%**
  fruit-pixel pointing       23.0%   ->   **96.2%**  (chance 5%)
  mean tree relevancy IoU    0.230   ->   **0.702**
  per-tree IoU                  --        0.794 / 0.443 / 0.734 / 0.835
**Conclusion:** the 5-level hierarchy survives rasterization. 95.8% of canopy pixels
identify their own tree out of 103; 96.2% of true fruit pixels identify their own
fruit node out of 20. Every earlier diagnosis built on the F4 numbers ("field
undertrained", "cos 0.989 too imprecise", "residual == inter-target gap", "target
separation is the ceiling") was an ARTIFACT of a checkpoint saved in a transient bad
state — retract those.
**Remaining real issue:** fruit-ANCHOR relevancy is still bimodal (0.393 / 0.095 /
0.004 across trees) while fruit-PIXEL POINTING is 96.2% — i.e. the field is right and
the softmax-vs-LERF-negatives scoring is what fails on certain fruit vocab words
(matches the ceiling-run finding). Fix candidate: negatives-free relative scoring
(Paul's earlier instinct), or reselect fruit vocab for separation.
**Artifacts:** /tmp/ladder/FULL (all 10 ckpts kept), /tmp/ladder_relevancy,
/tmp/ladder_full.log (per-step in-loop loss).

## 2026-07-27 — LEVEL COLLAPSE: fruit level is NOT learned (Paul called it)
**Paul's challenge:** the fruit and tree relevancy heat maps are near-identical
(16.4% vs 18.0% of px above 0.6) — "96.2 pointing is hiding a catastrophic failure".
Correct. Measured on the CLEAN 20k ckpt:
  TARGET radii (depth == radius): trees 4.69, fruit **7.15**  (fruit correctly deeper)
  RENDERED radii: fruit px **4.174** (should be 7.14), tree px 4.236 (correct)
  CROSS-LEVEL pointing, all 123 anchors competing:
     fruit px choosing a FRUIT anchor: **0.0%**
     tree  px choosing a TREE  anchor: 100.0%
So fruit pixels render at TREE depth and NEVER pick a fruit node when trees compete.
The 96.2% "fruit-pixel pointing" only ranked the 20 fruit anchors against each other;
since fruit node k is angularly aligned with tree k, a pixel sitting at tree k wins
for the wrong reason. Retract it as evidence of fruit localisation.
**MECHANISM — measured, not assumed.** Traced individual gaussians that sit on fruit
in kf543 (561 such, depth-checked) through all 123 frames:
  gaussian 522794: FRUIT 1 frame, TREE 7  | 848823: 1/8 | 796003: 1/11
           1161041: 1/3 | 875562: 1/5 | 1084823: 1/8
Each orange's gaussians are labelled fruit in ~ONE frame and tree in 3-11. SAM3's
tree mask covers the whole canopy incl. the oranges, so wherever the fruit pass did
not fire those same gaussians are painted tree. One fruit vote cannot pull a gaussian
to radius 7.14 against several tree votes pulling to 4.2.
**SECOND conflict found in the same table:** tree ids disagree across views — the same
gaussian is tree 73 in most frames and tree 84 in others (overlapping SAM3 canopy
masks). A within-level conflict under the between-level one.
**FIX (paint stage, not loss):** resolve each gaussian's label ACROSS VIEWS before
painting — deepest level wins (ever-fruit => always fruit), tree-vs-tree by majority.
Ancestors are then recovered by the interpolation walk, which is what it is for.
Testable on block_001 in one training run. Script: /tmp/gaussian_label_conflict.py.
**CORRECTION (Paul caught it) — occlusion check changes the tally.** My first
cross-frame trace read the label at the projected pixel WITHOUT testing whether the
gaussian was actually visible there, so occluded projections were miscounted as
"tree". Redone with a depth test (|projected − rendered depth| < 0.25 m):
  gaussian  FRUIT  TREE  unlab  OCCLUDED  offscreen   tree ids (visible only)
   522794     1     2     13      14        82        [(84,2)]
   848823     1     2     13      14        82        [(84,2)]
   796003     1    11     16       5        79        [(73,8),(84,3)]
   1161041    1     2     22       4        83        [(84,1),(73,1)]
   875562     1     4      7      18        82        [(84,2),(73,2)]
   1084823    1     1     13      15        82        [(84,1)]
Revised: the conflict is REAL but much smaller than first reported (1 fruit vs 1–4
tree for 5 of 6; one outlier at 1 vs 11). Roughly HALF the earlier "tree frames" were
occluded views. Also large "unlab" counts (7–22) — many visible views leave these
points unpainted entirely.
**=> The dominant cause of fruit-level collapse is therefore SCARCITY, not vote
conflict:** ~3,800 fruit px vs ~300,000 tree px per frame (~1% of the gradient), and
each orange is painted in ~1 frame while being visible in many. Retract "outvoted
50:1" and "1-against-7"; the honest statement is ~1-against-2 plus 1% pixel share.
**Fix ordering revised:** (a) up-weight fruit pixels in the geodesic loss (cheap, no
3D needed, directly targets scarcity) BEFORE (b) multi-view fruit label persistence
(back-project fruit masks through rendered depth, or SAM3 video-mode fruit tracking).
Still-true facts: fruit px render at radius 4.174 vs target 7.14; cross-level pointing
of fruit px onto fruit anchors = 0.0%; tree level is healthy (95.8%/99.1%, IoU 0.702).

## 2026-07-28/30 — fruit S-ladder: protected semantic class (S1→S2→S3a)
**Backfilled 2026-07-31.** Design (Paul): NO two-stage; gaussians must "always
choose fruit" — a protected semantic class. Mechanism: per-gaussian supervision
tally read from rasterizer blending weights via two autograd.grad probes on the
rendered feature image (S1 = Σ ind·⟨pred,u⟩ → mass m; S2 = Σ ind·⟨pred,target⟩ →
weighted target sum V); protection is sticky (m ≥ tau=10 over ≥2 frames); anchor
loss ‖f − (V/m).detach()‖² at weight 5.0 pulls protected gaussians to their own
supervising node word (not a generic fruit word). Occlusion handled for free by
transmittance in the blending weights.
- **S1** (tally in model buffers): gates FAIL — fruit radius 4.98 (target ~7.1),
  cross-level pointing 1.1%. Cause: densification resets buffers (35–100
  protected gaussians total).
- **S2** (protected class active): pointing 12.0%, radius 4.58. Protection works
  where it fires; coverage still reset by densify.
- **S3a** (tally buffers moved into gsplat strategy_state so ops.duplicate/split/
  remove cat them with the same selection as params — children inherit
  protection): 44k protected, pointing 21.9%, radius 4.91. Tree pointing 99.9%
  intact. Ladder: F4 0.0% → S1 1.1% → S2 12.0% → S3a 21.9%.
Ops scars: gsplat JIT OOM vs concurrent NerfInterface (MAX_JOBS=4 + serialize);
--max-iter 8000 < ckpt step 20000 silently trains 0 steps (exit 0!); queue kill
propagated through timeout killed S3a at 41% → stages now separate setsid units.

## 2026-07-29/31 — klapmuts: rows via HighInterface RANSAC; pilot post-mortem
**Backfilled 2026-07-31.** HighInterface (aru_nerf_interface) commit b1ea3054:
ransac_init = sequential RANSAC only with per-model inlier attribution (0.07 s);
fit_rows = init + real CoralOptimiser. Findings: num_models=40 is a MAX not a
min-points gate; RANSAC threshold ≠ labelling outlier threshold (mismatch made
45% outliers); dir gate 0.7 admits 45° diagonal thieves. Tuned dir 0.985 +
thr = row_spacing/2 → **14 clean rows, 0.12 s**. CORAL EnergyMinimisation is a
no-op at production λ=β=0 (5000 fixed iterations) and collapses to 1 model at
λ=β=0.5 → init IS the row solver here. Stage C rebuilt CORAL-free
(save_semantic_pngs_fast.py: pbImage protobuf + timestamp match, ~30 min → s).
Pilot block_004 splat FAILED usefully: eval 9.9 declining while train 18.2
rising; LiDAR depth exonerated (depth loss 0.36→0.026); render shows floater
curtain on the camera path (unmasked sky/translucent roof) and eval set was ONE
frame. Fix queued: sky/fg masks + real eval split before the 10-block fleet.

## 2026-07-31 — blanket supervision exposed; strict filter; S4 launched
Paul: "the localisation is soooo bad" → forensics on kf_000546. SAM3 'fruit'
prompt at score≥0.1/area≤20000 produced CANOPY-SCALE masks; training matched its
supervision (geodesic 0.526 vs 6.087 separation) → **labels, not model, are the
fruit-localisation blocker**. SAM3 score = instance prob × whole-image presence
(sam3_image_processor.py:192-198) → threshold must be per-frame-relative.
**Strict filter** (area 300–3000 px, fill≥0.45, aspect≤2.2, score≥0.5×frame-max)
on kf_546: 85 masks → 5, all individual oranges (scores 0.70–0.77 vs frame
median 0.109 — bimodal; fig lab_notebook/figs/strict_masks_kf546.png).
**Supervision v2_S** (paint_v2S_strict.py, ~/logs): 2,850 raw → 25 painted on
13/113 frames; **46 strict-true oranges dropped on trees absent from the 20-node
fruit inventory** → inventory undercount signal, revisit post-probe.
**RUN: S4** = S3a recipe + semantic_v2_S (automation/s4_strict.sh, log
~/logs/s4_strict.log, out block_001/splat_runs_S4/fruit_s4_strict). One variable
changed (labels). Gates rebaseline: numbers NOT comparable to blanket-label
ladder. NVML broken (driver/library mismatch; CUDA fine) — nvidia-smi blind
until next reboot. Paul's architecture call: retire "repaint" — supervision
compiler (uint16 id-maps + manifest: pinned id→word table, id→level table,
ledger hashes, drop accounting); colours demoted to viz-only. Plan agreed in
session; build pending S4 verdict.
