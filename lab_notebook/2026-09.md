
## 2026-09-01 — FG-PSNR prod-recipe plan: P0 baseline (4-block sample)
Bars: med≥21, P5≥17, int-min≥16, worst-vs-med≤4, verdicts no-regress, 2×-repeat |Δ|med<1.
| block | field | med | P5 | int-min | worst-vs-med |
| b013 | 0.45 seed, no chain | 21.28 | 16.37 | 14.88 | 6.4 ✗ |
| b040 | 0.6 + chain | 21.58 | 16.71 | 15.45 | 6.1 ✗ |
| b000 | era-repair chain | 21.41 | 16.16 | 14.28 | 7.1 ✗ |
| b026 | 08-24 fleet seed | 20.93 | **18.54** | **17.47** | **3.5 ✓** |
Findings: (1) the never-chained old-recipe block has the healthiest floors — supports L3 (chain splits to the last step, zero polish). (2) **b026 tree containment is DEAD (0.009/0.007; row2 0.757 survives)** — the "fruitless blocks' tree decode survives era re-convergence" assumption is FALSE; the 11 fruitless blocks need reseeding against the current embedder regardless of PSNR. (3) b013's census SEED alone verdicts tree 71 at 0.927, row 2 at 0.895 — the 17k chain train is for fruit, not tree/row containment. Baseline: scratchpad/P0_baseline.json.

**P2 (2026-09-01).** Disk-expand reboot wiped /tmp incl. the scratchpad harness + per-frame P0/P1 JSONs (summaries survive above; fields on disk unaffected — b40/b0/b26 re-measured bit-identical, determinism confirmed). Harness now durable: `automation/judge_recipe.py`, profiles land in `lab_notebook/data/psnr_profiles/`. **P2a verdict: min-range stays 0.45** — the 0.40 seed on b013 traded −0.9 median for +0.3 floors (inside roll noise). b013's 0.45 lineage restored as newest. P2b (b040 no-tail vs tail-16000 A/B for the fruit-vs-floors trade) running.

## 2026-09-01 — PSNR prod recipe CLOSED: P1–P4 results, recipe shipped
**P1 (polish tail, STOP_SPLIT=16000):** b040 first-ever all-bars pass (med 22.33, int-min 19.37, wvm 3.0); sample Δmed +0.4..+0.75 with 43 frames improved ≥2 dB. **P2a:** min-range stays 0.45 (0.40 traded −0.9 med for +0.3 floors — noise). **P2b (fruit-vs-tail A/B):** no-tail fruit verdict 0.567 < tail's 0.636 — the 0.962 was roll luck (spread 0.57–0.96 across rolls); tail wins BOTH axes. **P3 (culls): rejected on evidence** — geometric cull fixed worst (+8.3) but −2.6 collateral on median frames; view-exclusivity cull harmless but no effect (tails are multi-view co-adapted, not single-view junk). **P4 (gated re-roll):** b000 F→ALL-PASS on one re-roll (int-min 15.32→20.08, wvm 2.0); b026 floors pass on every re-roll (int-min 16.23/16.59) but med is content character (20.62–20.93 over 4 rolls, never 21) → exits flagged (exit 3), the designed behavior.
**Shipped:** `automation/prod_block_recipe.sh` (init 0.45 → stage1 15001/6000 → census seed → chain STOP_SPLIT=16000 → judge_recipe QA gate → bounded re-roll; exit 3 = flagged) + `automation/judge_recipe.py`; profiles land in `lab_notebook/data/psnr_profiles/`. Reboot-proofing: harness+profiles now repo-resident.
**Open (Paul):** (1) median/wvm bars mis-fire on dim-content blocks (b026 evidence) — proposal: keep absolute floors, make med relative (≥ best-known − 0.3); (2) L5 holdout policy (every-10th vs fixed 3-frame eval set) — untouched, still his call. dec_2025_ten_rows: transferred (67G), NOT touched per instruction.

**P4 + close (2026-09-01 11:4x).** P2b A/B: no-tail fruit verdict 0.567 < tail's 0.636 — the celebrated 0.962 was roll luck (b040 fruit spread across same-family rolls: 0.57–0.96); **STOP_SPLIT=16000 wins both axes** and enters the recipe. P4 gated re-roll demo: **b000 F/F floors -> ALL BARS PASS on one re-roll** (med 22.07, P5 20.72, int-min 20.08, wvm 2.0). b026 over 4 independent rolls: floors converge to pass (int-min 16.59, wvm 4.0 on roll 3) but median lands 20.6–20.9 every roll — block character, not variance; the absolute med≥21 bar mis-fires on this class. Repeatability (b026 roll2 vs roll3, n=72): |Δ| median **0.40 dB**, P90 1.43, max 3.79 — central repeatability holds; a 3-frame ±2–4 dB tail is inherent to the trainer (future lever: opacity-reset schedule), which is exactly what the gate+re-roll absorbs.
**RECIPE (automation/prod_block_recipe.sh, committed):** min-range-0.45 init → stage1 15001/6000 → census seed → chain STOP_SPLIT=16000 → judge_recipe QA gate → bounded re-roll → exit 3 flags. Sample outcome: b040 PASS, b000 PASS (1 re-roll), b013 3/4 (one roll-variance frame; passes under re-roll doctrine), b026 floors-pass/median-flag. **Open calibration question for Paul: make the median bar relative (≥ max(21, block-best − 0.3) no-regression form) — b026's 4-roll evidence.**

**A0 init ablation (Paul's experiment, b015, stage-1 only, 2026-09-01 19:4x).** Random init vs LiDAR init, all else identical: med 18.05 vs 21.84 (**−3.8**), P5 12.86 vs 19.84 (**−7.0**), int-min 11.42 vs 19.34 (**−7.9**), holdout-med **15.46 vs 12.96 (+2.5 — random overfits LESS)**. Verdicts: (1) LiDAR init is worth ~4 dB med / ~8 dB floors on trained views — not removable; (2) craters are NOT init-born (worst frames move: 1240/1207 vs 1174/1217 across inits); (3) **b015's LiDAR stage-1 field has NO crater (int-min 19.34, wvm 2.5) — its craters appear only after the fruit-chain leg**: the chain's 15000→17001 densify window is the crater factory on this block. Next mechanism lever = chain-side (schedule/opacity-reset inside the window), not init, not stage-1 re-rolls. (4) The +2.5 holdout gain under random init is the case for depth supervision / laxer splits as a generalization lever, someday.

**Reel final (2026-09-01 21:0x).** All 529 graded frames of the Tassili section ≥ 19 dB on visible (untinted) content — **min 19.67, median 22.73, zero below floor**. Mechanics: 19 dB keep-rule in the capture (9 flight views remapped to trained neighbours, 1 dropped), all five reel blocks on QA-gated recipe fields, pauses re-measured PER ROLL (fruit cuts are roll-specific — the fixed 0.88 misfired on the restaged b040, its own thr was 0.97 at eval / 0.90 live; shot moved to kf_003255 which now renders **23.18** — the frame this whole investigation started on at 13.6). Row-3 pause moved to kf_001086 (untinted 19.67); tree-68 to kf_001024. Ladder numbers in-reel: walk IoU 0.84/0.80/0.88/0.93/0.90, rows rec 0.990/0.986, fruit 0.42/0.40/0.41. Detour cost + lesson: b040's staged run was destroyed by "restore variant A by deleting variant B" — **staging REPLACES run dirs; restoration is always re-staging** (same law as the registry-path lesson). Ollama systemd service (9 GB) stopped for the train window (`systemctl start ollama` to restore).

## 2026-09-02 — sukuma (gendia) image-only pipeline: DA3-scene backbone + 3-arm init ablation
Sukuma end-to-end from bare images (238 frames, phone portrait). Eight portability fixes recorded (CUDA-13 lib for colmap, vendored read_write_model, py3.10 binding for ingest, prod/scratch_sam3 + prod/monos homes, kf_idx schema, rig-optional under --no-lidar, lio_image_poses.json pose fallback in kf_pose_records). SAM3 prompt battery: "terrace/crop row/ridge" fire on NOTHING; **"planter" (0.77–0.91) is the tower prompt**; "raised bed" segments individual tier rings; "plant" the crop. Hierarchy after Paul's two audits: 209 raw clusters → **25 compact towers** (≤5u of path, bbox ≤1u; far-field = DA3 range smear, unmeasurable from one aisle; mega-clusters = ground BEDS, a future first-class object). **DA3 scene mode is the geometric backbone**: pose-free 27-view pass = metric (1 COLMAP unit ≈ 1.10 m), 1.6 s; pose-conditioned windows fused ALL 238 views into one frame (1.63M pts, ~30 s GPU); towers resolve tier-by-tier in gravity-corrected elevation. **Init ablation (identical 5k-iter recipe, train/eval mean-last-1k): fused-238 14.15/13.85 > da3init-26 13.63/13.15 ≈ COLMAP-sparse 13.81/12.90** → fused init = image-recipe default. All arms ~6 dB below the osuga benchmark: scene is CAPTURE-limited (single-aisle walk, dense foliage, harsh sun) — render-grade gendia needs better capture, not more algorithm. nerf_new PIL/libtiff failure was transient (launcher LD-path), env verified clean.
## 2026-09-02 — M1 anchor decomposition complete: CPT fixes what Gemma 4 still lacks — delivery, not understanding
- **Run:** plans/multilingual_model_track.md M1. 3 arms (g3-12b-pt, AfriqueGemma-12B, g4-12b-it)
  × Belebele (100×8 langs, 2-shot) × FLORES (32×7×2 dirs, 3-shot), Q4_K_M constant.
  Base arms raw few-shot; g4 via its chat template (deployed plane, cross-plane caveat).
  Outputs experimental_multiling_m1/ + summary.txt. THREE g4 attempts: raw probe
  (degeneration loops), 2k budget (starved: 58% empty xh), final 4k/8k budgets — empties
  PERSIST (xh 49%/zu 38% MCQ, ~46% FLORES): silence is model behaviour, not budget.
- **adapt (afrique−g3), FINAL:** en→xx chrF +14.5 zu / +9.5 xh / +8.0 ha / +7.0 am; ~0 on
  af/sw/ar; xx→en and Belebele barely move. CPT buys GENERATION into low-resource langs.
- **g4 CONDITIONAL Belebele accuracy (answered only): xh 82% zu 84% (vs g3 base 58/66)** —
  Gemma 4 understands these languages BETTER than Gemma 3; it fails to DELIVER (silence)
  and its xh/zu generation trails CPT-g3 by 15–35 chrF. am/ar en→xx g4 actually +3.2/+1.9.
- **Fine-tune case, now scoped:** teach g4 delivery/generation in xh/zu(+ha), not
  comprehension. Cheap Adinkra mitigations exist today: per-language budget policy +
  no_answer honesty (already shipped). Ops lessons: dead-server poison rows (3 quarantine
  dirs keep the forensic trail), drivers now ensure_ollama; fertility table banked
  (am 1.59 chars/tok = Ge'ez byte-fallback; xh/zu ~1.5× en).
- AUTONOMY: runs=1 clean=0 interventions=2 debugged=3

**Sukuma ingestion A/B (Paul's call, 2026-09-02 20:1x): +6 dB.** The image-only SfM script had drifted from the validated configuration on THREE axes (found by diffing against osuga's artifacts): 608px working frames instead of width-1080 (my downscale misread), SIMPLE_RADIAL instead of OPENCV, sequential-only matching instead of exhaustive (no binding between the walk's out and back passes — every surface potentially doubled). Corrected ingestion (1080×1920 + OPENCV + exhaustive; 238/238, 120k pts, 1.2M obs) + DA3-fused init: **train 20.11, eval 18.06–19.91 vs 14.15/13.85** — at the osuga benchmark (20.41). Recipe consequence: colmap_glomap_auto.sh must be brought back to the validated config (OPENCV + exhaustive or vocab-tree loop closure + explicit width-1080 rule); the init ablation stands (fused-DA3 default). Attribution among the three axes unconfounded only as a bundle — decompose later if it matters.
## 2026-09-03 — g4pt bf16 column lands: NO base-level regression — the instruct layer eats delivery
- **Run:** unsloth/gemma-4-12b (true base, ungated mirror of gated google/gemma-4-12B) in
  bf16 via transformers 5.16.1, dedicated ~/envs/hfeval venv, hf_run.py on the SAME frozen
  slices/prompts as the ollama arms. 800+448 rows, ~3.5 h. Caveat: bf16 vs others' Q4.
- **gen_pt (g4pt−g3, controlled base-vs-base): modest POSITIVE everywhere** — Belebele
  xh +6 / zu +3 / am +9; chrF en→xx +1.8..+4.1; xx→en +0.5..+4.8. Gemma 4 base is a
  small, real multilingual improvement. The g3→g4 "regression" was never in the weights.
- **The instruct layer is the destroyer, now isolated:** g4-it vs g4pt, en→xx chrF
  xh 19.8 vs 43.5, zu 21.5 vs 44.3; xx→en collapses ~30 pts in EVERY language (even sw,
  am, ar). Reasoning-tuned chat behaviour, not language knowledge, kills delivery.
- **Ranking of levers for African generation:** African CPT (+9.5/+14.5 xh/zu) >> one
  model generation (+2.8/+4.1) — adaptation buys 3-4x what the upgrade did.
- **Fine-tune scoping, complete:** CPT gemma-4-base (slightly better start than
  AfriqueGemma's g3) for generation, THEN an instruct stage carrying African-language
  delivery data — a delivery-preserving instruct layer is not optional; it is where
  stock Gemma 4 loses these languages. AUTONOMY: runs=1 clean=1 interventions=0 debugged=0

## 2026-09-03 — sukuma COMPLETE: v2 markers→embedder→HiGH features→Bateleur; two root-caused bugs
Full v2 semantic chain landed: markers on merged-pass geometry (**28 towers, obs/tower 13→27, bbox 0.71→0.47 vs v1**), semantic PNGs non-empty 238/238 (v1: 172 — exhaustive matching binds the passes), embedder `sukuma_v2b` (retrieval 100% on 74 ids / 8 row-groups), HiGH feature train 5000 iters on full-res 1080×1920, export. **Measured (tensorboard, mean-last-1k): train 18.62, eval-images 18.60 (last 19.04)** — RGB-only twin was 20.11/19.91; the ~1.5 dB feature-train gap is UNATTRIBUTED (single roll each; feature-head capacity vs roll variance — decompose only if it matters). Artifacts: `v2/.../block_000/splats/splat.ply` (153 MB), splats.json/index.json, `ujamaa` 34836e7 = Bateleur P-01 "Sukuma Towers" (28 towers, 14.3 m path, metres ×1.10) live at :8001/bateleur.
**Bug 1 — ERRATUM on 09-02's "PIL/libtiff transient" line: it was never transient.** `train_hyperembedder.py` imported `aru_nerf_interface` before PIL; the binding pulls opencv → SYSTEM libjpeg.so.8 (no jpeg12_* symbols), and once that soname is claimed, PIL's conda libtiff.so.6 can't resolve `jpeg12_write_raw_data@LIBJPEG_8.0`. Deterministic import-order failure, `nm -D`-verified; "PIL alone imports fine" is exactly what the bug predicts, which is how it got misdiagnosed. Fix: PIL import above the binding (in-file, commented); memory `nerf-new-pil-import-order` written.
**Bug 2 — in-train eval OOM at full res.** Relevancy decode over a 1080×1920 eval render allocates ~16 GiB in one unchunked decoder linear (2.07 M px × 2048 × 4 B) — 720p citrus never tripped it. Fixed: chunked eval-only decode (256k px) in `high_model.py`, high repo LOCAL commit.
**Codified:** `automation/prod_image_recipe.sh` (width-1080 rule, OPENCV+exhaustive, DA3 all-views fused init, SfM ≥95% + hierarchy gates; header marks repeatability-run PENDING); DA3 fusion heredoc recovered verbatim from session transcript → `aru_sil_core/src/scripts/image_pipeline/da3_windows_fuse.py` (compile-checked; live rerun deferred to the repeatability run); farm-image-pipeline skill corrected on all four drift axes (scale rule, camera model+matcher, HIGH_EMBEDDER_CKPT env replaces the clip_encoder sed, fused init).
**Open:** twice-from-scratch repeatability run (acceptance bar for calling the image recipe prod); mango as second gendia sample (fruit ledger via DEPTH_DIR); render-grade sukuma needs better capture (unchanged); ollama still STOPPED.

**Sukuma query reel + instance-query finding (2026-09-03 12:1x).** Rendered the v2 HiGH field along its 238 survey views via BlockRenderer (540x960, native intrinsics) — RGB + containment-query overlay; reel at `ujamaa/demo/sukuma_query_reel.mp4` (f4e5420). Measured query behaviour: free text "planter" is FLAT (p50 0.511 / max 0.563 — untrained word, the expected level-blind glow); row-0 codeword separates cleanly (p50 0.54 vs p95+ 0.755–0.79, thr 0.7 tints tower stacks and excludes crops); **NO tower-instance codeword separates** — probed ids 0/2/7/44 ("asus/ought/secretariat/sank") on multiple frames incl. tower 44 (208 obs): all flat, max ≤ 0.50, exclusive scoring only zeroes background. So the sukuma field answers ROW queries but carries no usable per-tower identity. Hypothesis (unproven): 74 visually near-identical black towers give CLIP mask features nothing instance-specific to bind to leaf codewords — citrus trees are individually distinct, these aren't. Also noted: embedder's internal row grouping is 8 rows (markers_v2 derivation) vs the scene-graph hierarchy's 1 row — id spaces differ across the two surfaces; row 0 happens to cover the queried towers. Overlay ramp lesson: containment heat tops out ~0.03 above a working threshold — alpha must ramp over ~0.06, not (1−thr). Mid-path frames 90–110 show row-query spill (38–52% of pixels) — uninvestigated.

**Init A/B round 2 (Paul's hypothesis, 2026-09-03 12:4x–14:5x): COLMAP cloud beats fused-DA3; DA3 far-field smear was eroding stage-1.** Stage-1 RGB, 5k iters, identical flags; train/eval-img = tensorboard mean-last-1k. Baseline v2 fused-DA3 (1.9M pts): 20.11/19.91. **Arm A** same 238@1080×1920, init = v2 COLMAP sparse (120k pts): **23.27/21.38 (+3.2 train)**. **Arm B** ALL 713 originals @ native 2160×3840, own exhaustive SfM (713/713 registered; glomap 1h20m, matching 14 min) + its COLMAP cloud: **22.16/21.71**, 13 min train (0.116 s/it, cache=disk). A-vs-B not directly comparable (4× pixels/frame, 3× frames, different eval frames); the clean claim is COLMAP-init > fused-DA3-init on identical data, inverting v1 (13.81 < 14.15) — v1's sequential-matching cloud was junk so DA3 fill helped; v2's exhaustive cloud is clean so DA3's range smear only adds misplaced mass. Fix chain en route: ns-train host-OOM at 713×4K traced to HighDataManager's eager [N,3,H,W] float32 stack (~71 GB) that NO code consumes (HighDataloader reads the semantic stack) — deleted (high local commit); also config-provided semantic_dir pointing at an empty dir builds N black full-res canvases before concluding RGB-only — dropped the flag, auto-discovery path exits immediately. Recipe consequence (pending Paul + an osuga control, NOT run per instruction): image-recipe stage-1 init default flips to COLMAP sparse on corrected ingestions; DA3 retained for metric scale + clustering depth.

**Splat-depth reclustering A/B (2026-09-03 15:0x): DA3 keeps the clustering job.** Rendered metric depth from the arm-A COLMAP-init field at all 238 views (608×1080, world-units mm) and re-ran the identical clustering chain (same SAM3 masks, DBSCAN eps 0.2 / dist-gate 5 / min-samples 3; new flags --depth-dir/--out-name/--transforms-name on cluster_tree_instances_depth.py, --gid-name/--out-name on build_hierarchy_from_clusters.py — variant runs no longer clobber prod). Non-circular audit says splat depth is WORSE: towers 22 vs 28, obs/tower 30.1/20 vs 35.6/27 (mean/med), bbox-diag 0.57/0.56 vs 0.50/0.47, detections associated 4229 vs 4758, and rows fragment 1→4 (13/7/1/1). The ring→splat-mass median (0.016u vs 0.043u) flatters splat depth but is circular (measured against the same field). Labeled hypothesis for the deficit: composited gaussian depth blends fore/background on thin structures and is unreliable where the field is thin at range, while DA3's per-pixel depth is exactly its training task; also this field is a 13-min 5k-iter stage-1 — not metrology grade. Division of labour stands: COLMAP cloud → init; DA3 → metric scale + mask-lift depth; splat raster → Bateleur underlay + independent audit layer (ring-vs-footprint offsets made visible). Artifacts: v2/depth_png_splat/, global_ids_splatdepth.json, marker_hierarchy_splatdepth.json, query_video/topdown_depth_ab.png.

## 2026-09-04 — gendia fleet rebuild: NOT a pipeline bug — view-starvation + non-comparable baselines (self-correction)
Cleared all gendia surveys to source images, ran prod_image_recipe (COLMAP-sparse init) fleet-wide. Outcome + full diagnosis:
- **Results:** osuga train14.10/eval14.36, sukuma 14.00/13.07, osuga_baseline 14.25/13.23; mango+pawpaw FAILED at sky-mask stage. All ~14 → looked like a systematic regression vs osuga's May 20.41 and arm-A's 23.
- **Ruled out (with evidence), in order:** features-path (RGB-only osuga = 13.4, same); gaussian count (healthy 1.8M); pose↔image pairing (image_to_monolithic rename is internally consistent — block image_80 = raw image_170 in BOTH pose+pixels, verified vs read_write_model); SfM/poses (reproj filtered 1/126k; **init.ply reprojects onto real scene structure** — overlay confirms poses good); intrinsics (70° diagonal = normal phone, principal point centered is fine); resolution schedule (num_downscales=0 full-res-from-0 = 13.2, no better); **the high code path itself (plain nerfstudio splatfacto ALSO 13.8 on the same block)**.
- **Root finding:** NOT a bug. (1) osuga 30k-iter test: train 13.8→17.0 but **eval 12.8→11.8** — eval DEGRADES with more iters, train-eval gap 1.0→5.2 = **overfitting on a view-starved capture** (188 frames, every-8th eval ~8 walk-frames from nearest train view; sparse soil + wind-moving foliage). (2) The "good" baselines were never comparable: osuga's May 20.41 was at 608×1080 (old long-edge-1080 rule); arm-A's 23 was a half-res-schedule tensorboard train number. Tonight's ~14 is the honest full-res number.
- **Consequence for the init decision:** arm-A's "COLMAP +3.2 dB > DA3" was **TRAIN-only**. On a view-starved scene the metric that matters is EVAL, and the depth-prior literature (Mode-GS, In-Depth-We-Trust, CDGS) says depth regularization helps generalization precisely here — so the COLMAP>DA3 conclusion must be re-tested on EVAL before it's codified. NOT settled.
- **Fixes landed:** build_sky_masks.py + build_fg_masks.py mkdir(parents=True) (was crashing the two --sky surveys). Recipe's 5000-iter cap is fine for train but there's no iteration count that fixes eval on these captures — it's a capture-density limit.
- **Open decision (Paul):** these image-only gendia walks are STRUCTURE-grade (hierarchy/Bateleur top-down are unaffected — they use SAM3+DA3, not the splat) but not render-grade at full res without denser capture or depth regularization. mango+pawpaw NOT rerun pending direction.

## 2026-09-04 — GPU bundle-adjustment glomap built for the A100 (Paul's call)
The image pipeline's SfM was CPU-bound in bundle adjustment: `/usr/local/bin/glomap` (and its FetchContent'd colmap) were built for sm_70 on the V100 and inherited by this A100 box; glomap logged `Requested to use GPU for bundle adjustment, but COLMAP was compiled without CUDA support. Falling back to CPU-based solvers.` On the all-1412 osuga run this made a single BA iteration take 25–85+ min (killed after 8.5 h, never wrote sparse).
**Root cause (source-verified):** glomap's estimators force `ceres::SPARSE_SCHUR`, whose only GPU path is `ceres::CUDA_SPARSE` = NVIDIA **cuDSS**, gated on **Ceres ≥ 2.3**. The box had only Ceres 2.2 (CPU-only) and cuDSS ships **cuda12-only**, while the only complete dev toolkit was CUDA 11.8 — so GPU BA required a CUDA-12/Ceres-2.3/cuDSS stack, not a flag.
**Built (all in isolated prefixes; `/usr/local/bin/{colmap,glomap}` untouched):** CUDA toolkit 12.4.1 (`/home/paperspace/code/_cuda12`), cuDSS 0.4.0.2_cuda12 (`/home/paperspace/code/_deps/libcudss-*`), abseil 20240116.2 (`_deps/absl-install`), Ceres main@fe351d5 (v2.3.0-dev, WITH_CUDA, sm_80) at `ceres23/install`, glomap at **`/home/paperspace/code/glomap/build_gpu/glomap/glomap`** + its consistent colmap 3.14.0.dev0 at `build_gpu/_deps/colmap-build/src/colmap/exe/colmap`. apt: `libgoogle-glog-dev`+`libunwind-dev` (were genuinely missing).
**Runtime LD_LIBRARY_PATH (required):** `/home/paperspace/code/_cuda12/lib:/home/paperspace/code/_deps/libcudss-linux-x86_64-0.4.0.2_cuda12-archive/lib:/home/paperspace/code/_deps/absl-install/lib:/usr/local/lib`
**Verified:** `ldd` links libcudss.so.0/cusolver/cusparse/cublas/cudart.12; NO "Falling back" line; osuga-188 GPU vs CPU-forced — global-positioner sparse solve 28.8s vs 52.5s (1.82×), total recon 81.7s vs 111s (1.36×); win scales super-linearly with image count (the sparse solve dominates).
**Gotchas for wiring in:** (1) the GPU glomap's colmap (3.14-dev, commit b6b7b54) **cannot read** database.db written by the pipeline's CUDA-13 colmap (SQLite schema mismatch) → feature+match must be redone with the matched colmap (`build_gpu/_deps/.../colmap`); (2) colmap 3.14-dev renamed flags: `--FeatureExtraction.use_gpu` / `--FeatureMatching.use_gpu` (not `--Sift*`); (3) Eigen must be 3.4 (`-DEigen3_DIR=/usr/share/eigen3/cmake`); (4) one guard patch in the cuDSS archive's cudss-config.cmake for CMake 3.22 nested-scope. Full cmake configure lines for ceres23 + glomap saved in this session's build report. Reproducible-recipe / recipe-wiring is a follow-up (osuga all-1412 rerun on this stack in flight to get the view-density eval answer).

## 2026-09-05 — dec_2025_ten_rows: whole-survey LiDAR cloud on the A100 (camera-frame odometry handled)
**Goal (Paul):** the laser pointcloud for the entire dataset; "the transforms are from the camera — think it through before executing."
**State found:** this box held the 72 GB mcap + rig.json + STALE .index files only — no monolithic streams; the V100 (where the 08-18 converted poses live) was unreachable. Ingest had to be redone here. Stale V100 indexes deleted first (a real-sized stale index reads as silent garbage; the 1-byte one as empty).
**Ingest:** canonical `a300_bag_to_monolithics.py` under nerf_new python3.10 (aru_py_logger cp310 + rosbags live there; miniconda lacks protobuf/ROS). One edit to the canonical tool: `cloud_xyzir` now parses PointCloud2 with numpy from the message's own field table (no ROS `sensor_msgs` on this box) — validated on a real scan first (64k pts, 32 rings, x/y/z/intensity f32 @0/4/8/12, ring u16 @16, step 26). **13.9 min** (V100: 32.2); every count == the bag's connection table (image_left/right/depth 5903, laser 3943, zed_odom 5914, ins 11829, gnss 395). **OPEN: depth.monolithic is 0 B despite 5903 records reported** — the script never `del`s its loggers (image_to_monolithic does, "# flush"), but image/laser flushed fine so this looks like a silent DepthPredictionLogger write failure; unverified, not needed for the cloud.
**Frame contract:** `ensure_laser_frame_poses.py --data-dir prod/monos` (prod layout: MD=monolithics/, rig at prod/monos): detected `zed_transform n=5914 spread x42.1 y2.3 z49.9 -> camera frame`, conjugated per increment (T_lidar = L2C^-1 T_cam L2C) -> `spread x41.9 y49.7 z3.9 -> laser`; wrote transform_lio_laserframe.monolithic (550,002 B = same record count as the odom). The two `transform_lio` symlinks it creates were REMOVED (no-shims; the accumulator references the laserframe file explicitly and passes the rig's A300 L2C — the binding otherwise falls back to the Jackal extrinsic).
**Accumulator design (measured):** `LaserProjector.project()` returns only the camera-frustum subset (~15.6k of 64k/scan; 37k 'behind', 12k 'outside'); `project_debug()['p_world']` carries the FULL scan and matches an independent lift `c2w @ L2C @ T_motion @ raw` to 1.8e-5. Images 15 Hz vs scans 10 Hz -> ~1.5 images match each scan, so we lift ONE image per scan (min |dt|, median 17 ms). Edge frames return `error: no LIO pose at ts_img` (1 skipped). `c2w` absent on those -> guard it.
**Result:** 3,937/3,943 scans lifted, 249.6M returns within rmax 40 m -> **67.4M voxels @ 5 cm**, PLY 1.28 GB at prod/tassili/ten_rows_laser_cloud.ply (+ .npz). Trajectory flat axis y (camera-world) = EXPECTED. Height: ground -1.4 m below sensor, p98 +4.95 m; 56% of voxels within ±2 m of the sensor plane. Within 10 m of the trajectory: 56.3M voxels, extent **62.0 x 21.0 x 69.8 m vs 08-18's 52.3 x 4.5 x 61.5** — ground plane ~10 m larger each way = the full-360° lift vs that run's frustum-only points; no streaking/vertical smear (the camera-frame failure signature). Accumulate 7.7 min CPU, no GPU.
**Scripts:** ~/logs/tenrows_{ingest,post_ingest}.sh, tenrows_laser_cloud.py, tenrows_topdown.py (to codify under automation/ once the kf-cut/semantic follow-on is decided).
**ten_rows cloud — rows visible (2026-09-05, follow-on).** Top-down at rmax 40 with max-height colouring is a 120 m smeared disc (far-field returns from every scan; radial gradient = coverage, not geometry) — useless for rows but the camera path drawn over it is a clean 5-pass lawnmower (the frame-fix evidence: a wrong conjugation streaks exactly this). Near-field view (within 12 m of path, plant band above ground): **return DENSITY shows the ten rows as parallel bands along the path legs**, rover threading the aisles; concentric rings at the start = stationary start scans. Max-height in a 0.15–3.0 m band saturates everywhere → a layer at ~2.5–3 m above ground across the block (canopy or overhead structure; unidentified). Band 0.15–1.8 m mean-height + a perpendicular-to-legs histogram gives 11 peaks at ~2.5 m typical spacing but with gaps — the 1-D detector is confounded (rover-proximity returns, slight row/path tilt); the density image is the reliable evidence, not the peak count. Ground = 1.40 m below the sensor. Renders: prod/tassili/ten_rows_laser_{topdown,rows,rows_v3}.png; scripts ~/logs/tenrows_topdown{,_v2,_v3}.py.
**ten_rows trajectory + ZED yaw drift (2026-09-06).** Trajectory-only render (prod/tassili/ten_rows_trajectory.png): 3,937 frames, 479 m path, 42.1 x 49.9 m footprint, 2.32 m vertical drift, **10 legs** of 45–48 m, successive perpendicular spacing alternating ~3.5 m / ~5.5 m. Leg ORIENTATIONS rotate with time in the ZED odometry — outbound and return groups each monotonic, ~16–17° first-to-last (spread 21.4°, corr(leg idx, orientation) −0.63). Discriminator against the INS (GNSS-aided, ins_transform.monolithic, 11,829 poses, same increment convention, analysed in its own frame): 10 legs, spread 11.2°, corr +0.04 — NO time trend; its two direction-groups sit at a constant ~8° offset (a fixed out/back heading bias, not drift). **Verdict: rows are physically parallel; ZED visual odometry yaw-drifts ~17° over the 479 m survey.** Consequences: (1) the whole-survey LiDAR cloud built on ZED poses is sheared first-to-last by up to ~17° (rows still parallel LOCALLY — which is why the density view looked clean); (2) ZED's footprint reads 7 m wider in x than INS (42.1 vs 34.5) from the fanning; (3) for census-grade geometry the pose source should be INS-anchored (absolute heading, ENU0/WGS84 per 08-14) — but rig.json carries no INS→lidar extrinsic, so a cloud on INS poses needs either that calibration or a drift correction of ZED fitted to INS headings (time-varying yaw). Scripts: ~/logs/tenrows_zed_vs_ins.py; figure prod/tassili/ten_rows_zed_vs_ins.png.
**ten_rows lidar→camera extrinsic is measurably off (Paul's suspicion, 2026-09-06).** Extrinsic used everywhere: `prod/monos/rig.json` `laser_to_camera_left` (rig klapmuts_a300_dec2025, R ≈ diag-flip, |t| 0.44 m: lidar 0.24 m above / 0.37 m behind the left camera), passed as `l2c=` — verified the projector applies exactly it (0.0002 px vs an independent projection). Test = lidar depth vs ZED `depth_registered` (camera-native, pulled per-frame straight from the mcap since the ingested depth stream is 0 B) at the projected pixel. **Rig L2C: floor-region residual median +0.58…+0.71 m on 4 frames, ≤16% within 0.2 m** — a coherent geometric error (foliage is parallax noise). Convention candidates: the INVERSE does not fix it (rotation is involutory, so only the lever arm flips; floor points leave the floor region), zero/negated translation are far worse (+1.4…+1.5 m) → the matrix's direction and lever arm are right, the ROTATION is off. Derivative-free refinement (Powell, soft-L1 0.3 m, bilinear ZED sampling — nearest-pixel sampling zeroes the Jacobian, which is why the first least_squares attempts silently did nothing / diverged) joint over f500/1000/2500/4000: **correction rotvec [+3.24, −1.01, −0.65]° (3.45°), translation Δ ≤ 2 cm**; all-point median +0.23 → −0.01 m (33 → 55% within 0.2), floor +0.67 → +0.20. **Held-out** f1500/f3500/f5000: +0.70/+0.10/+0.19 → +0.28/−0.00/−0.02 (floor f5000 +0.58 → +0.12). Remaining +0.1…+0.4 m floor bias on some frames unexplained (ZED far-floor bias vs further extrinsic terms; translation is weakly constrained by depth-only). Refined matrix saved as `prod/tassili/l2c_refined_zedfit.json` (+ .npy), status candidate — rig.json NOT modified. Consequence: every scan in the whole-survey cloud carries the ~3.2° pitch error (≈0.1–0.3 m near-field row shift, ≈0.5 m ground height error at 10 m) on top of the ZED yaw drift; both need addressing before census-grade use. Renders: prod/tassili/ten_rows_proj_check_f1000.png, ten_rows_extrinsic_fit_f*.png, ten_rows_extrinsic_rig_vs_refined_f1000.png; scripts ~/logs/tenrows_proj_check.py, tenrows_extrinsic_fit{,2,3}.py, tenrows_extrinsic_show.py.
**ZED refined with INS (Paul: "ins pointcloud or refine zed with ins", 2026-09-06).** Chose refinement over an INS-native cloud: rig.json has no INS→lidar extrinsic (an INS cloud would add a new unknown calibration); refinement uses only the INS heading over time and keeps ZED's local structure + the validated camera-frame contract. Method: per-LEG orientations matched leg-for-leg in time (10 legs each) — the instantaneous velocity-heading route fails here (ZED unwrapped heading spans 1801° = 9 U-turns + drift, INS 418°; heading-RATE correlation only r=−0.40 at 30 Hz) — INS ground plane is MIRRORED vs camera-world (mirror sign −1 gives per-leg residual spread 11.5° vs 31.7°). Leg drift ZED−INS: 0, −0.3, −2.4, −2.5, −5.2, −5.5, −9.9, −9.6, −11.5, −10.3° (monotonic, ~11.5° over 479 m — less than the ~17° raw-spread estimate, which included the constant out/back alternation). Applied as a per-increment yaw about the camera vertical (dead reckoning with corrected heading; yaw sign chosen empirically: wrong sign gives spread 31.2°). **Result: corrected ZED legs spread 12.5°, trend −0.09 (raw 21.4°/−0.63; INS 11.2°/+0.04); footprint 36.4×47.0 m vs INS 34.5×47.4 (raw 42.1×49.9 — the 7 m fanning excess is gone).** Conjugation regression vs ensure_laser_frame_poses' stream: 2.98e-8. Streams: monolithics/zed_transform_inscorr.monolithic (camera-frame, provenance) + transform_lio_laserframe_inscorr.monolithic (projector input). Gotchas: scipy uniform_filter1d propagates a single NaN through its whole output (running-sum) — never feed it NaN gaps; ensure_laser_frame_poses.read_transforms returns (msg, matrix) tuples. Script ~/logs/tenrows_zed_inscorr.py; figure prod/tassili/ten_rows_zed_inscorr.png. Drift-corrected cloud accumulation in flight (rig extrinsic kept so the pose correction is the only variable).
**Drift-corrected cloud built (2026-09-06).** Same accumulator, same rig extrinsic, pose stream = transform_lio_laserframe_inscorr: 3,937/3,938 scans, 249.6M returns → **66.85M voxels** (raw-pose cloud 67.42M — fewer distinct voxels = less smear, more overlap), extent 115.7 × 31.6 × 126.8 m (raw 121.6 × 31.7 × 129.3). Projected-path legs: raw 21.4° spread / trend −0.63 → corrected **12.4° / −0.09** (INS 11.2° / +0.04); footprint 42.1×49.9 → 36.4×47.0 m (INS 34.5×47.4). Perpendicular row comb on the corrected cloud: 12 peaks, median spacing 2.75 m (detector still gappy — heuristic; density image is the evidence). Artifacts: prod/tassili/ten_rows_laser_cloud_inscorr.{ply,npz}, ten_rows_laser_rows_inscorr.png, ten_rows_rows_raw_vs_inscorr.png (side-by-side, identical scales). Still carrying the ~3.2° rig-extrinsic pitch error (candidate refined matrix not adopted) — the two corrections are independent and composable.
**ten_rows block budget (Paul: "how many blocks / how much GPU time", 2026-09-06).** No blocks built yet (prod/tassili/blocks_ns/lio_row100 is empty). Row partition simulated on the drift-corrected path with the pipeline's own rule (ROW_MAX_KF=100): 1,765 kf at 20 cm, 10 legs of 161–188 kf → 2 blocks/leg → **20 row blocks of ~88 kf** (arc-15 m alternative ≈ 36). Measured per-block GPU time at the same 1280×720 resolution, from citrus 01–05 run dirs (config mtime → last events mtime): **stage-1 15,000 iters = median 7.2 min (4.8–12.6, n=296, median 79 kf/block)**; whole prod chain per block from the fleet lane logs (stage-1 → census → chain → judge, incl. queue overhead): **median 17–21 min** (laneA n=105 median 17; pre0823 n=32 median 21), long tail 3–8 h on re-rolled/fruit-chain blocks. Survey-level GPU: SAM3 ≈ 0.31 s/frame (osuga 188 frames/58 s) → ~9 min for 1,765 kf; embedder ~1 min; LiDAR init is CPU. ⇒ ten_rows ≈ **2.4 h GPU for stage-1 across 20 blocks**, **~6–7 h for the full prod recipe** (up to ~10 h with QA re-rolls), +~15 min survey-level. Caveat: the G0 QLoRA fine-tune currently holds 30.7 GB / 100% util (ETA ~12 days), leaving 8.6 GB — citrus chain trainers used up to 29 GB, so the block fleet cannot run concurrently with it.

## 2026-09-06 — ten_rows: QLoRA paused at a checkpoint; 30 row blocks + LiDAR init; keyframe-vs-full-frame A/B armed (IN PROGRESS)
**Ask (Paul):** pause the language fine-tune at a resumable point, then run stage-1 on dec_2025_ten_rows; in one block, A/B keyframes vs full frames (PSNR).

**Pause (resumable):** `run_g0.sh train` is an auto-relaunch loop (`until TRAIN_DONE … sleep 600; retry`), so the watcher kills the wrapper (PID 317022) *before* the trainer (317027). It waits for `g0_run/checkpoint-450` to be complete (trainer_state + rng_state + adapter) and size-stable 120 s. Step 386/4864 at 10:47 box time, 241 s/step → checkpoint-450 ≈ 15:05 box. Resume: `bash automation/g0_qlora/run_g0.sh train` (adds `--resume` itself). Log `~/logs/g0_pause.log`.

**Poses promoted:** `monolithics/transform_lio.monolithic` := the INS-drift-corrected laser-frame stream (real file, 550002 B; the contract every tool reads). Extrinsic = rig.json as-is (the ~3.2° pitch error stands until Paul adopts `prod/tassili/l2c_refined_zedfit.json`).

**Keyframe cut (20 cm, camera translation on the corrected stream):** 5903 images → **1992 keyframes**; `image_left_kf20cm.monolithic` (2.96 GB, verbatim pixels, original ts) + `kf_index.json`; PNGs `prod/scratch_sam3/kf_%06d.png`. Two-process write (reader = aru_nerf_interface, writer = aru_py_logger) to avoid the double-binding protobuf clash. Gotcha: a timed-out first dump left one truncated PNG that a size>0 skip check kept — validate with `Image.load()`.

**Blocks:** `build_row_blocks.py --max-kf 100` → **30 blocks** (23–98 kf; 1761/1992 kf in blocks; turns dropped). rig.json intrinsics (fx 529.40 fy 529.65 cx 647.20 cy 354.64, 1280×720, OPENCV). The partition auto-wired `mask_path` to a STALE `prod/tassili/fg_masks/` (1790 PNGs from the August cut — different kf numbering) → stripped all 1610 mask_path entries; dir renamed `fg_masks_STALE_aug_kfcut`.

**Two latent bugs fixed in `lidar_init_per_block.py` (disk-only, aru_sil_core):** (1) it never passed `l2c=` → the binding's JACKAL extrinsic on any rig (now `l2c_flat(root)`; no-op on citrus whose rig.json equals the Jackal numbers); (2) `mono()` did not resolve `prod/monos/monolithics/` → all 30 blocks "frames with no kf image" on the first pass.

**A/B design (block_013, 98 kf, one full pass):** test = every 8th kf (13), val = test; kf arm trains on the other 85 kf; full arm trains on every image_left frame in the same time span (258 in 17.2 s) **minus 38 near-duplicates inside the test keyframes' windows** → 220 frames (2.59× the kf arm, median dt 67 ms). Full-arm poses = the `poses_at_keyframes` formula at image timestamps (replication error 0.0 at the kf timestamps); same `init_lidar.ply`; explicit `train/val/test_filenames` (absolute paths — the dataparser matches `_get_fname` outputs exactly).

**Stage-1 flags (ten_rows):** citrus stage-1 minus supervision (no TREE_WEIGHT), `sky-loss-lambda 0.0` (polytunnel roof), `report-masked-metrics False`, 15001 iters, antialiased, stop-split 6000; driver `~/logs/tenrows_stage1_driver.sh` gated on PAUSE DONE → A/B pair first, then the other 29 blocks; PSNR table `~/logs/tenrows_stage1_psnr.tsv`. Convention note: the blocks carry OpenCV-style camera axes (cam-y·world-down = 0.997) exactly as the citrus chain does — consistent by construction, not re-derived.

**Erratum (same day, before any GPU run):** the first partition + init were built on WRONG block poses. `survey_paths.load_l2c` reads `monos(d)/rig.json`; for klapmuts `monos()` resolves to the nested `prod/monos/monolithics/` (no rig.json there) → silent JACKAL fallback for the pose conjugation, while the projector used the A300 rig via `rig_calib`. Same stream, two extrinsics → the block world was a rotated copy of the projector's (positions x↔z swapped; e.g. K50: projector [-0.33, 0.33, 11.29] vs blocks [11.27, 0.82, -0.06]) → `LIDAR-init: N raw -> 0 after-AABB` on every block away from the origin. Fixed `load_l2c` (checks the parent dir; warns on fallback; verified == rig). Blocks, A/B arm and inits rebuilt (`~/logs/tenrows_rebuild_chain.sh`). The keyframe cut itself is unaffected (it used the camera stream directly). Lesson: any "silent default" for a rig quantity is a bug — `load_l2c` now prints when it falls back.
**After the fix:** `build_row_blocks.py --max-kf 100` on the correct poses gives **23 blocks** (the wrong poses had spurious direction reversals → 30). Inits now keep 75–83 % after the AABB (e.g. block_000: 1.48 M raw → 1.23 M AABB → 569 k voxel; block_004: 892 k → 653 k → 316 k) vs 0–21 k before. A/B block_013 becomes 100 kf: 87 train-kf vs 233 full-stream train frames (2.68×), 13 shared held-out keyframes.
**Pause fired 15:03 box time:** `checkpoint-450` complete (3.15 GB, global_step 450, train loss 1.978, lr 9.87e-5); wrapper + trainer stopped, GPU back to the 1.6 GB viewer process. Resume = `bash automation/g0_qlora/run_g0.sh train` (auto `--resume` → checkpoint-450). Stage-1 driver took the card at 15:04:27; block_013 kf arm trains at ~21–26 ms/iter (4.1 GB), split override confirmed in the dataparser log (train skips every 8th kf; val = the 13 held-out). Note `steps_per_eval_all_images=25000` > 15001 iters → the tensorboard eval tag is a single random held-out image every 500 steps; headline A/B numbers come from `ns-eval` over all 13 held-out keyframes (`~/logs/tenrows_ns_eval.sh`).

**A/B result and why it is not the headline (15:30 box):** block_013, identical 13 held-out keyframes, `ns-eval` over all of them: keyframes (87 train) **12.11 dB** / SSIM 0.367 / LPIPS 0.769; full frames (233 train) **11.98 dB** / 0.398 / 0.825 (Δ −0.13 dB); train PSNR 22.16 vs 18.84; wall 367 s vs 404 s. Held-out renders are floater mush 20 cm from trained views (figure `prod/tassili/block_013_evalimg_step15000_img.png`), and 233 views at 67 ms spacing collapse the same way → geometric inconsistency, not view starvation. Driver stopped by hand at 15:22 after the pair.
**Convention finding (contract violation, real but not sufficient):** every consumer of block `transforms.json` treats it as OpenGL c2w (`view_tree.opengl_to_opencv`, `instantsplat_setup`, `render_fidelity_gate --convention gl`, nerfstudio itself), and the legacy `nerf_per_block.py` wrote it via `opencv_to_opengl`; `build_row_blocks.py` (since 2026-08-13) writes the OpenCV camera-frame pose straight through. The LiDAR-init projection test (`block_013_axes_test.png`) confirms the row-block poses are OpenCV c2w. **Fixed in `build_row_blocks.py` (writes `T @ diag(1,-1,-1,1)`, tags `pose_convention: opengl_c2w`).** Empirically, though, retraining the kf arm with flipped poses gives only **13.11 dB** (SSIM 0.379, LPIPS 0.725, train 22.63; 358 s, 15 ms/iter vs 24) — +1.0 dB, still mush. A citrus lio_row100 stage-1 on this box (01_13B block_000, 2026-08-24) shows the same held-out smear (eval psnr_fg 13.3 vs train psnr_fg 21.3; `citrus_ref_evalimg.png`), so the row-block chain has had this since August; the C++/`nerf_per_block` lio_arc blocks (18.6 dB FG eval in the 08 notebook) did not. Next: COLMAP on the same 100 keyframes as an independent pose reference (hypothesis: the pose stream itself is inconsistent frame-to-frame).
**Root cause found against an independent reference (15:40 box):** COLMAP (GPU SIFT + exhaustive + incremental mapper, calibrated intrinsics fixed) on block_013's 100 keyframes: 100/100 registered in 126 s. Sim(3)-aligned to our poses: scale 1.886, position residual p50 0.18 m (RMS 0.99 m from a few outliers); **consecutive-keyframe rotations agree** (COLMAP 1.10°/3.25° vs ours 1.02°/3.21° median/p90, |diff| median 0.38°) → the jitter is real vehicle motion; **but every frame's orientation is off by ~173°**: camera-axis dots ours·COLMAP = x −0.992, y −0.990, z +0.997, residual a proper rotation (det +0.81) about the optical axis → **our stored camera frame is rolled 180° about z relative to the images** (camera faces the direction of travel — confirmed by ORB radial flow, 66–86 % expanding). Same jitter in the raw `zed_transform` stream, so the INS correction is not involved. Hypothesis for the cause (untested): physically inverted ZED with image auto-flip while odometry stays in the sensor frame. The LiDAR projection/extrinsic tests could not see this (they lift and project with the same pose), and the L2C itself is right (single-frame image alignment). Consequence: the survey LiDAR cloud was lifted with rolled cameras → re-lift after the fix. Test arm `block_013_fix`: `T·diag(-1,-1,1)` then the GL flip, init = COLMAP sparse mapped into our world via the Sim(3). `block_colmap_refine.py` is unusable on this box (system `/usr/local/bin/colmap` links a missing `libcudart.so.13`).
**Reference ceiling (15:42 box):** same 87/13 split on **COLMAP poses** (+ COLMAP sparse init): ns-eval **17.72 dB** / SSIM 0.529 / LPIPS 0.493, train 24.24 (361 s). So the images are fine and ~18 dB is what this block gives at 15 k iters with correct poses; ours-as-given 12.11, GL-flip-only 13.11. Roll-corrected arm (`block_013_fix`, ours·diag(-1,-1,1) → GL, COLMAP init mapped into our world by the Sim(3)) training next; block_014 (return leg) SfM running to confirm the error is per-camera, not a world rotation.
**Independent corroboration (15:47 box):** re-deriving the INS drift correction on the roll-corrected ZED stream (`inc' = C·inc·C`, C = diag(-1,-1,1)): the INS "mirror sign" that the 09-06 morning fit had to force to −1 now comes out **+1** (spread 11.5° vs 31.7° for −1), same leg drift 0→11.5°, corrected legs spread 12.5° vs INS 11.2°, heading residual 0.01°. The mirrored-INS-ground-plane oddity was this bug's fingerprint: the stored camera world was a mirror image in the ground plane. Streams: `monolithics/{zed_transform_rollfix, zed_transform_rollfix_inscorr, transform_lio_laserframe_rollfix_inscorr}.monolithic` (`~/logs/tenrows_rollfix.py`; `tenrows_zed_inscorr.py` now takes `ZED_IN`/`OUT_SUFFIX`). Keyframe selection is unaffected (a global 180° world rotation preserves distances). Survey LiDAR cloud and all 23 inits must be re-lifted.
**Roll-corrected arm (15:48 box):** `block_013_fix` ns-eval **15.13 dB** / SSIM 0.409 / LPIPS 0.638 (348 s) — vs 12.11 as-given, 13.11 GL-only, **17.72 COLMAP-pose ceiling**. The roll fix recovers ~3 dB of the 5.6 dB gap; a ~7° residual orientation offset vs COLMAP (172.7° measured, not 180°) remains — being characterised (constant camera-side offset → calibration term; scatter → odometry noise). block_014 confirms the same per-camera roll (x −0.998, y −0.998, z +0.997).
**Residual after the roll fix, vs COLMAP (block_013 / block_014):** per-frame orientation scatter about the mean median 0.92° / 1.23°, p90 2.57° / 3.46° (odometry orientation noise: 9–30 px at fx 529 — this is what caps the roll-fixed arm at 15.1 dB); mean offset 7.7° / 3.0° whose roll component (6.9° / 1.6°) is an artifact of a straight pass leaving roll unconstrained in the position alignment, while pitch recurs (−3.3° / −2.5°, hypothesis: fixed pose-frame→image pitch; untested). Translation is good: high-frequency position residual p50 8–10 mm, p90 32–38 mm; the 0.11–0.18 m p50 is low-frequency drift/scale. Remedy for the scatter = image-based refinement (per-block COLMAP Sim(3)-aligned into the LIO world, the `block_colmap_refine` idea; or in-loop `camera-optimizer SO3xR3`). Launched 15:52: fleet rebuild + driver on the roll-corrected stream (Paul's run; A/B first), camera-optimizer test on block_013_fix, COLMAP SfM over all 23 blocks in the background for a refined-pose pass.
**16:03 box — fleet relaunched on corrected poses; side tests:** rebuild on the roll-corrected stream: 23 blocks, inits 246k–500k voxel points (median 387k), block_013 re-validated against COLMAP end-to-end (axes +0.992/+0.990/+0.997, residual 8.07° = the constant offset). Driver relaunched 15:58 (A/B pair first). **In-loop camera optimiser (`camera-optimizer.mode SO3xR3`) on the roll-fixed block: 11.41 dB — worse than without (15.13); rejected.** Survey cloud re-lifted: 49.6 M voxels (66.9 M before — the rolled lift double-placed returns), ground −1.20 m, 12 row peaks, spacing median 2.50 m (`ten_rows_laser_rows_rollfix.png`). Pitch/yaw-offset arm (`block_013_fixp`, extra −3.27°/+0.76°) queued; COLMAP SfM over all blocks running (1/23 at 16:03).
**Pitch/yaw-offset arm (16:14 box):** `block_013_fixp` (roll fix + constant −3.27° pitch / +0.76° yaw from the COLMAP residual) → ns-eval **15.23 dB** / SSIM 0.410 / LPIPS 0.620 vs 15.13 without: +0.1 dB, inside noise → the recurring ~−3° pitch is NOT a usable calibration term; the residual vs COLMAP is per-frame scatter. Not adopted. Corrected fleet: block_013 kf arm done 16:05 (wall 454 s on a shared GPU; train 22.80, tb single-image eval 14.6–15.0); full arm training.

**A/B keyframes vs full frames — on corrected poses (16:16 box), block_013, same 13 held-out keyframes, 15 001 iters, LiDAR init, sky loss off, no supervision:**
| arm | train views | train PSNR (last 1k) | ns-eval PSNR | SSIM | LPIPS | wall (shared GPU) |
|---|---|---|---|---|---|---|
| keyframes @20 cm | 87 | 22.80 | **14.43** | 0.399 | 0.659 | 454 s |
| full stream @67 ms (−38 near-test dupes) | 233 (2.68×) | 20.04 | **15.24** | 0.443 | 0.716 | 589 s |
Δ(full − kf) = **+0.81 dB PSNR, +0.044 SSIM, LPIPS worse by 0.057** at equal iterations (the full arm sees each image 2.7× fewer times: train PSNR 20.0 vs 22.8). Single run each — run-to-run noise unmeasured. Note the fleet kf arm (LiDAR init) scores 14.43 vs 15.13 for the same poses with the COLMAP init mapped into our world (`block_013_fix`): init worth ~0.7 dB here (n=1). Both arms remain ~2.5–3 dB under the COLMAP-pose ceiling (17.72) because of ZED orientation scatter. Figures: `block_013_ab_report_rollfix.png`.
**Refined-pose test (16:30 box):** `block_013_ref` = COLMAP poses Sim(3)-aligned into our roll-corrected world (scale 1.886, position p50 0.18 m), same split, **LiDAR init kept**: ns-eval **17.52 dB** / SSIM 0.513 / LPIPS 0.504 (680 s on a shared GPU) — within 0.2 dB of the COLMAP-native ceiling (17.72), **+3.1 dB over the fleet's keyframe arm (14.43)**. So the remaining loss is entirely odometry orientation scatter and per-block SfM refinement (`~/logs/tenrows_colmap_axes.sh` + `tenrows_refine_block.py`, ~2–3 min/block) removes it. Refined-pose fleet pass queued after the odometry-pose fleet (`tenrows_stage1_driver_ref.sh`).
**16:33 box — GPU contention decision:** my side tests (40k equal-epoch arm, full-stream COLMAP chain) had slowed the fleet 2.3× (56 vs 25 ms/iter); stopped both and re-queued them after the fleet (`~/logs/tenrows_side_queue.sh`, gated on STAGE1-ALL DONE). Fleet wall times for block_000 (668 s) and block_001 are contended; later blocks run alone (~6 min). Refined-pose fleet (`tenrows_stage1_driver_ref.sh`) gated on SfM-all (14/23 at 16:33) + fleet done. Scripts codified under `automation/tenrows/` (082ec18).
**Per-block SfM (16:35 box, 14/23 done):** on the rebuilt blocks the camera-x agreement with COLMAP is +0.99–1.00 everywhere the model is sound (roll fix confirmed fleet-wide; position p50 2–8 cm on good blocks). COLMAP's incremental mapper FAILED on block_003 (2/67), block_005 (2/94), block_006 (2/93), block_012 (10/100) and produced a bad model on block_008 (75/75 but p50 1.03 m, x-dot 0.75). `tenrows_refine_block.py` now refuses coverage <90 % or alignment p50 >0.5 m; `tenrows_sfm_retry.sh` re-maps the bad blocks with the GPU GLOMAP global mapper after SfM-all (before the refined fleet needs them). Fleet unimpeded again: block_001 at 15.9 ms/iter.
**Per-block SfM complete (16:52 box):** COLMAP incremental mapper, calibrated intrinsics fixed, exhaustive matching, ~2–3 min/block on the GPU stack. **12/23 sound** (≥90 % registered, Sim(3) position p50 median 0.054 m, camera-x agreement median +0.999 — the roll fix holds on every sound block). Failed to register: 003, 005, 006, 016 (2/N), 012 (10/100); registered but degenerate (wrong merges on repetitive plant texture): 008 (p50 1.03 m), 014 (1.72 m), 019 (3.76 m), 022 (0.79 m); 013's second run 0.40 m (the first, `colmap_b013`, 0.18 m, is the one used). block_017 produced no output (investigating). GLOMAP (global) retry running on the 10 bad blocks. Fleet 6/24 at 16:52, ~6.5 min/block alone.
**GLOMAP retry (17:03 box):** the GPU GLOMAP global mapper on the same databases recovered **all 10** blocks the incremental mapper failed or mangled: 100 % registered, Sim(3) position p50 1.4–10.2 cm, camera-x agreement +0.994–0.999 (003 0.019 m, 005 0.102, 006 0.041, 008 0.014, 012 0.038, 013 0.048, 014 0.059, 016 0.073, 019 0.023, 022 0.027). Lesson: for repetitive-texture row passes use GLOMAP, not COLMAP's incremental mapper (which failed/mangled 10 of 23). `tenrows_refine_block.py` now takes `colmap_<blk>` for every block (block_013 included — 0.048 m beats the incremental 0.18 m used in the 17.52 dB test). block_017 (no initial pair) being GLOMAP-mapped.
**17:04 box:** block_017 also recovered by GLOMAP (70/70, p50 0.018 m, x-dot +0.999) → **23/23 blocks have sound image-based pose models**; the refined-pose fleet will cover every block (block_013 retrained on its GLOMAP model rather than carrying the incremental-model 17.52 dB). Odometry fleet 8/24.

**Odometry-pose fleet complete (18:47 box) — stage-1, 23 row blocks, roll-corrected + INS-corrected ZED poses, LiDAR init, sky loss off, no supervision, 15 001 iters, `--eval-mode interval 10` (block_013: explicit 13-kf split).** Held-out ns-eval PSNR over all eval views: **median 15.25 dB (min 13.97 block_019, max 18.08 block_017)**; SSIM 0.39–0.60; LPIPS 0.50–0.67; train PSNR 21.9–26.3. Wall: **median 356 s per block alone** (322–370 s for 67–100 kf; block_000/001 668/547 s while sharing the GPU); total 2.45 h GPU for the fleet. Table `~/logs/tenrows_stage1_psnr.tsv` + per-block `stage1_eval.json`; figure `prod/tassili/stage1_blocks_report.png`. Answer to "GPU time to complete the dataset" (stage-1 only): 23 × ~6 min ≈ 2.3 h on the A100-40GB; the full chain (SAM3 → markers → embedder → HiGH) is not included. Side queue (40k equal-epoch arm, refined full arm) and the refined-pose fleet started 18:45.

**A/B completed on both pose qualities (19:24 box):** (a) full frames at **equal epochs** (40 001 iters, 1334 s shared GPU): ns-eval 15.22 dB / SSIM 0.410 / LPIPS 0.642 vs 15.24 / 0.443 / 0.716 at 15 k → extra iterations do not raise PSNR, they only close the LPIPS gap (under-training). (b) **Refined poses**: GLOMAP on all 246 frames of block_013's span (SfM 484 s), Sim(3)-aligned into our world (p50 0.082 m), LiDAR init, 15 k iters (610 s): full stream **17.65 dB / 0.518 / 0.561** vs refined keyframe arm 17.52 / 0.513 / 0.504 → **+0.13 dB, a wash** (LPIPS worse). Conclusion: on odometry poses the full stream gains +0.8 dB by averaging pose noise; with good poses, 20 cm keyframes already carry the information and the full stream costs 2.7× per epoch for nothing. Refined fleet: 4/23 done at 19:24 (505–513 s while sharing the GPU), now alone → ~21:30 box.
